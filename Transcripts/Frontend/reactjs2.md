Sneha Mehra (00:00:00)  
So the very first thing that I'm going to need to do with this project is actually create a VT project. So let's just come in here. We're going to create an after folder. I'm going to CD into that after folder. I'm going to run npm create vit at latest. I'm going to put a period here to say that I want to create it in this folder. I want to create a React project. We're going to use JavaScript swc. And then I'm just going to install the dependencies and run dev to actually run our server. So npm rundev. I'll hit enter on that as well.

And if we just give it a second, it should run all that. And there we go. I have my project open up on the side. And let's get rid of all the stuff that we don't need. So we can get rid of that, that, that. And let's just get rid of all the unnecessary imports that we have. There we go. And this right here should just return null. There we go. So the very first thing we need to do based on our instructions is fetch all the users from this API. So I'm going to copy this link right here. And we're going to come into our page. And we're just going to do a simple use effect.

Because we know that use effect is the best way to fetch data, and we want to run a fetch request that goes to that URL. So let me just fix all the different stuff here. So there we go. We're fetching from that URL. ⁓ And then what I want to do is I want to get the response from that and convert it to JSON, because this is a JSON API. And then I want to set my actual users information to that. So let's get some state. ⁓ We're going to ⁓ pass in an empty array just like that.

Now if we give that a quick save, you're going to notice nothing changes because we're not rendering out anything to our page, but it's working with no errors. So the next thing we can do is go to step two, render an H1 that says user list, and below that a UL containing all the different users. And we want these users to be in their own component, and that component should take a name prop and return the name inside of an LI. So relatively straightforward. We want to come in here and we want to return an H1 ⁓ that says user list. There we go.

Now if we save, you can see we get that H1, but we also want to be able to return a UL. So in our case, we're going to be using a fragment so we can return both of these without having to wrap them inside of anything complex. So we're going to have our UL as well. And inside the UL, we want to loop through each one of our different users. So I can come in here, users.map, and I want to map through each one of our different users. So we can say user. ⁓ And then I want to return some code, which is going to be a custom user component where I pass in the name, ⁓ which is our user. Whoops.

Sneha Mehra (00:02:20)  
User dot name. There we go. And if I just make sure I close off everything. There we go. Give it a save. You can see that we are getting our code to work. There's no errors on our code, but we have some errors in our console. If we check over here, you can see we have a bunch of errors. The main one is user is not defined yet. So let's create that component. ⁓ User.jsx. ⁓ Export user takes in a name. ⁓ And in here I want to return an LI that has our name. There we go.

Whoops, close that off. ⁓ Obviously I did this wrong. There we go. ⁓ So we should have an LI that has our name inside of it, relatively straightforward. So our user is now defined. Now all we need to do is import that user. ⁓ User ⁓ from dot slash user. There we go. And now you can see our list is populating for us. Now if by default our users was not an array, so if we left it undefined like this, you're gonna see we're gonna get an error. And if we inspect our page, we can see that it says cannot read properties of undefined.

And that's because here users is undefined, and then we set it a little bit later. So you would need to do a simple check where you said users is not equal to null, and then run this code, and that's going to work just fine. So it's only going to run this code if we have some users. But in our case, we're just going to set this to an empty array by default. So we don't even need to worry about this because by default it'll be an empty array and there's nothing to map over. Now that mostly covers the stuff that we need to fix, but we have a few other problems if we inspect our page. I'm just going to clear out my console and refresh.

You're gonna notice this says each child needs to have a unique key. So if you remember properly, we need to give these a key. And luckily, in our case, we have a user.id that we can use as the key. So now if we save and refresh, you're gonna notice we no longer get any errors in our console, which is ideal. Now if we go back to the README, you're gonna notice last thing left is the bonus section for adding a loading state, and we just need to add the text loading instead of our user list if it is loading. So to do that is actually relatively straightforward. There's two ways we can do it. The first is with some if statements.

And the second is with some inline stuff. So I'm going to show you the inline way of doing things because that's what we covered in this section of the course. And it's probably the most common way you would see this problem solved. So what we need to do is first get a loading state. So let's go ahead and do that. We're going to say ⁓ is loading. Set ⁓ is loading. And by default, loading is actually going to be set to true because we're going to be fetching immediately when we run our page. And also, this use effect should be having an empty bracket here. That way we don't refetch every single time the page reloads.

Sneha Mehra (00:04:47)  
Obviously that's not ideal. So here we have that empty brackets right there. And now we need to come in here and we just need to say dot finally.

And here we can set loading to false. So set is loading is going to be set to false right here. And whenever we do a request, I want to set is loading to true. So every time we make a request, is loading is going to be set to true. And every time we finish it, we set is loading equal to false. Also, I should probably deal with canceling inside of here as well. So I'm going to have a signal. ⁓ And that signal is going to be from our controller.signal. ⁓ And to get a controller, we can just create a controller, which is a new.

abort controller. We covered this in the last section of the course. ⁓ So down here, whoops, we can return a function that just calls controller.abort. There we go. So now what happens is if we for some reason re-render this component twice back to back, such as in strict mode, it's going to make sure it aborts the first request before going to the second request. So now we have our isLoading state so we can use that down here. And in ⁓ do it right here, we can say is loading

What we want to do is we want to render out the loading text. I'm going to put that inside of an H2. I believe that's what it's said to do. It didn't say to do anything, so we'll just do an H2 in our case. ⁓ Loading. Otherwise, if we're not loading, I want to render out this entire UL. So I'm just going copy this up and put it right there and remove the old one. Click save. And now you can see everything's working. But it's hard to test because our page is so fast to load. So I'm going to change our network speed here to a slow 3G. And then with that done, I'm just going to come over here, give this a quick save, and give it a quick refresh.

And now you can see we get that text loading showing up. And if we just wait for the page to load and download everything, again you'll see that text loading shows up for a little bit. And then it's going to show the actual elements inside of our list. Obviously, it takes a long time to download everything in development because we are using that slow 3G connection. So it's really throttling us. But any minute now, it should hopefully start showing our different section loading. There you go. You can see it says loading. And then it shows up the actual user. So that's exactly what we want. And let's just get rid of this throttling so we're not slowed down forever.

Sneha Mehra (00:06:51)  
Now, the one other thing I want to mention is if for some reason this API was different for you, the way to get around this is to create a public folder. We're just gonna call it public. And I just want to take this user JSON, copy it over into our public folder. And then what we're doing, our fetch request, just fetch that users file. So we can say users.json. And this is going to work exactly the same. So now if I save, you can see when I refresh, I still get that exact same list of users. So this is going to be for this project as well as any other project that uses an API. If I have a JSON file that I provide.

Just put it in the public folder and then fetch it just like this. For our use case though, if the API is working, obviously just use the API. ⁓

—------------------

Sneha Mehra (00:00:00)  
Now before we go any further into learning React, we first need to understand what React even is. Now if we go to the React documentation, you can see that it says React is a JavaScript library for building user interfaces. That is the simplest definition you can possibly give React, and it's not the most helpful thing in the world. In my opinion, React is a library that just makes it much easier to write complex interactions on a site. If you're building out a really simple site that only has a few different interactions on it, you can just use plain JavaScript.

When your site gets more and more complex and you have to reuse a lot of concepts and logic across your application, for example, you're building like Facebook or Twitter, these applications are massive, they become difficult to maintain. And as you want to do more and more stuff on the front end, you want to make all these changes on the front end instead of pushing back and forth between a server, you're going to lean more and more towards a library like React. And React falls under the umbrella of a front-end framework. Now, there's a lot of people that get mad and say React isn't a framework, React's only a library.

I don't think what any of those people have to say is that important. It doesn't matter whether you call it a library, a framework, it's just something that helps you write front-end code. And there's other front-end frameworks, there's like Vue and Svelte and Angular, and React just happens to be the most popular and the one that I actually really enjoy working with. Now one important thing to understand about React is it's probably one of the closest front end frameworks to just plain JavaScript, but they do a lot of things with JavaScript to make it seem kind of magical what actually happens. There's no fancy magic steps behind the scenes.

There's a lot of really complex things that React does behind the scenes to make writing the code a little bit more seamless and a little bit easier. So of course we're going to cover all those different things so we can kind of pull back the curtain on what's happening behind all that to make it easier to understand. But really React is just a JavaScript-based framework or library that helps you write out complex front ends, and that's really where you're gonna want to use it whenever you have some type of complex front end.

Another important thing to understand about React is of all the front end frameworks I mentioned, it is definitely the most bare bones. It includes the least amount of features. For example, there's no routing based in, no authentication, none of that complex features that you're going to find in a lot of other front end frameworks. And this is a blessing and a curse. It's nice because React is smaller and you can choose what you want, but it's also a bit of a pain because it's difficult to find the exact thing that you need, because there's 10 options you could choose from. In this course, I'm going to be focusing most exclusively on React.

Sneha Mehra (00:02:15)  
We're gonna cover some of the libraries that you're gonna wanna add into React if you're doing more complex things, but really the majority of this course is gonna focus on React itself. And I'm really only going to focus on the libraries that are absolutely must-haves in pretty much every application. Another couple important things to understand about what React is, is it's a declarative component-based library. And this is really important because everything is broken up into components. I actually have a video in this section just a little bit later where we're really gonna talk about components and what they all mean.

But it just means everything's broken into small chunks that you can reuse over different places. And it's also declarative instead of imperative. And really all that means is that normal JavaScript, it's imperative. Like you say, do this, then this, then this, then this, then this. And with React, you're trying to change that mindset and you're saying, This is my data, do this with the data. So like you're changing the data, and the data changes how the page actually looks. You're declaring what the page should look like based on whatever data gets passed in. While with normal JavaScript, you're saying, okay, do this, then do this, then do this.

And it has nothing to do with this like back-end data model. Again, we're gonna talk a lot more about this as we start building out our first React projects, but it's just something to keep in the back of your mind.

—---------------------------------

Sneha Mehra (00:00:00)  
Now before we go any further into learning React, we first need to understand what React even is. Now if we go to the React documentation, you can see that it says React is a JavaScript library for building user interfaces. That is the simplest definition you can possibly give React, and it's not the most helpful thing in the world. In my opinion, React is a library that just makes it much easier to write complex interactions on a site. If you're building out a really simple site that only has a few different interactions on it, you can just use plain JavaScript.

When your site gets more and more complex and you have to reuse a lot of concepts and logic across your application, for example, you're building like Facebook or Twitter, these applications are massive, they become difficult to maintain. And as you want to do more and more stuff on the front end, you want to make all these changes on the front end instead of pushing back and forth between a server, you're going to lean more and more towards a library like React. And React falls under the umbrella of a front-end framework. Now, there's a lot of people that get mad and say React isn't a framework, React's only a library.

I don't think what any of those people have to say is that important. It doesn't matter whether you call it a library, a framework, it's just something that helps you write front-end code. And there's other front-end frameworks, there's like Vue and Svelte and Angular, and React just happens to be the most popular and the one that I actually really enjoy working with. Now one important thing to understand about React is it's probably one of the closest front end frameworks to just plain JavaScript, but they do a lot of things with JavaScript to make it seem kind of magical what actually happens. There's no fancy magic steps behind the scenes.

There's a lot of really complex things that React does behind the scenes to make writing the code a little bit more seamless and a little bit easier. So of course we're going to cover all those different things so we can kind of pull back the curtain on what's happening behind all that to make it easier to understand. But really React is just a JavaScript-based framework or library that helps you write out complex front ends, and that's really where you're gonna want to use it whenever you have some type of complex front end.

Another important thing to understand about React is of all the front end frameworks I mentioned, it is definitely the most bare bones. It includes the least amount of features. For example, there's no routing based in, no authentication, none of that complex features that you're going to find in a lot of other front end frameworks. And this is a blessing and a curse. It's nice because React is smaller and you can choose what you want, but it's also a bit of a pain because it's difficult to find the exact thing that you need, because there's 10 options you could choose from. In this course, I'm going to be focusing most exclusively on React.

Sneha Mehra (00:02:15)  
We're gonna cover some of the libraries that you're gonna wanna add into React if you're doing more complex things, but really the majority of this course is gonna focus on React itself. And I'm really only going to focus on the libraries that are absolutely must-haves in pretty much every application. Another couple important things to understand about what React is, is it's a declarative component-based library. And this is really important because everything is broken up into components. I actually have a video in this section just a little bit later where we're really gonna talk about components and what they all mean.

But it just means everything's broken into small chunks that you can reuse over different places. And it's also declarative instead of imperative. And really all that means is that normal JavaScript, it's imperative. Like you say, do this, then this, then this, then this, then this. And with React, you're trying to change that mindset and you're saying, This is my data, do this with the data. So like you're changing the data, and the data changes how the page actually looks. You're declaring what the page should look like based on whatever data gets passed in. While with normal JavaScript, you're saying, okay, do this, then do this, then do this.

And it has nothing to do with this like back-end data model. Again, we're gonna talk a lot more about this as we start building out our first React projects, but it's just something to keep in the back of your mind.

—-------------------

Sneha Mehra (00:00:00)  
Now I know watching that last video may have been a little depressing talking about how difficult React is and how much of a journey it is to learn it, but do not be discouraged by that. Learning React is incredibly enjoyable and rewarding, and it's something that I know for a fact that you can do. It's not beyond anybody, and you definitely have the abilities to learn React. Now, one of the things that is the most discouraging for people is the fact that they see other people succeeding. You're gonna go to social media, you're gonna go to YouTube, and you're gonna see other people building out these crazy React projects.

They do it in what seems like no time at all, and you're still struggling to write a to-do list. That's okay. Everyone's on different stages of their journey. Some people have been doing React since the day it came out, and you're just starting today. So you shouldn't be as good as them and you shouldn't expect yourself to be as good as them. Another thing that really discourages people is actually watching my videos and seeing how quickly I build out different projects. And you start to think, wow, this guy builds these projects super quick, he never makes any mistakes, I'm never gonna be that good. And that's because I'm not even as good as I look in these videos.

And that's because whenever I build out a project in any of the videos in this course, I'm just copying code that I have already written. I've already built out the entire project beforehand, gone through all the debugging process, made all the mistakes already, so that I know for a fact what I need to write and what I need to talk about. I do this because it makes the viewing experience for you much, much smoother. So when you're watching me build out a project, just know it's not my first time building the project. I have the code open on my second monitor so I can constantly check to make sure I'm doing things correctly.

And if I end up doing making a mistake, which almost always happens, I just cut that mistake out so you never even see it. Now there are points in this course where I will deliberately or accidentally make a mistake and I will leave it into the video. And I do that because it's really important to see how to recover from different mistakes and how to debug issues. One of the things you're gonna do all the time as a programmer is debug problems from mistakes that you are made or from things that you're doing that you're not really sure about.

So I like to leave in some of these mistakes or deliberately make certain mistakes in order to show you how to get out of those problems and debug common issues that'll come up. So if it's taking you 10 hours to build out a project that only took me 30 minutes to build in the video, just know that it probably also took me 10 hours to build that behind the scenes and you just didn't see any of that process because I like to simplify things and make it as small and short as possible. So I'm not wasting your entire time trying to learn React. I know the only reason you're watching this course is because you want to learn React, it's not because you want to listen to me talk.

Sneha Mehra (00:02:22)  
So I'm trying to make this course as short as possible while giving you all of the knowledge that you need. So just remember, it doesn't matter how long it takes you to learn React, it doesn't matter how much quicker or slower you are than the people that you know. Just know that as long as you're constantly making progress and you're learning React, that's all that really matters because eventually you're gonna get to the point where you feel confident with React. And that's really where I want you to get by the end of this course. I know this is gonna be a difficult journey, but I am incredibly excited to lead you down this journey and finally get you to the point where you're mastering React.

—-------------------

Sneha Mehra (00:00:00)  
Now that we understand the rules of hooks, we can actually start diving into some more niche hooks. And the very first hook I want to talk about is the use ref hook. And the whole idea behind this hook is to be able to persist data between different renders, but have that data not actually be tied to state or props. Because we already know if our state changes or if our props change, our component is going to re-render. But sometimes we want to store data but be able to change that data without causing our component to re-render. And that's the whole idea to the use ref hook. So we can just take this use ref hook right here.

We can import it. And then to use the use ref hook, all we do is we call use ref and we just need to pass it a default value. So for example, I could just pass it a string that says kile and this will be the default value. Now it's important to understand that use ref and use state both work exactly the same. So I can pass it a value, or I can pass it a function that is evaluated to get the value and they both do the exact same thing. So whether you want to pass a function or you want to pass just a value like this, either way is going to work.

And then you have a reference to that. So we're going to say that this is going to be our Kyle ref, just because we gave it the value of Kyle. Just set it like that. So really all this use ref hook is doing is it's wrapping a particular value and it's putting it inside of an object. And if we look at this object, you can see that this object has one property called current, and that's just going to give me the current value of whatever I stored. So in our case, the current value is Kyle. So if I console.log this real quick, you can see that this is just going to give me the text of Kyle. If I inspect my page, go over to the console, you can see it's just printing out Kyle.

So our ref is just always storing the current value. And if I wanted to change that value, for example, I come in here and I say kile ⁓ ref.current is equal to Sally and I change it to Sally. Now when I inspect my page, you can see that it's being set to Sally here. If I just make sure I change this refresh, you can see it's being set to Sally. The important thing though is this does not cause my component to re-render. Let's just come in here with a use effect. ⁓ And I want this use effect to render run every single time our component re-renders, and we'll just say re-render.

So now if we inspect our page and we come over here, you can see if we just do a quick refresh, you can see it's printing out Sally and it's printing out re-render because of the very first time I render my page out. Now let's come in here and let's actually change this on button click. So we're just going to create a simple button. Do it outside of this label here.

Sneha Mehra (00:02:11)  
Button on click.

Sneha Mehra (00:02:16)  
And we're just going to say kile ref dot current is going to be equal to Sally. Or actually, let's just set it to a random number. That way it's always going to change every single time we click this. Math dot random. There we go. It's going to be set to a completely random number every single time we click on this button. And every single time our component renders, I'm logging out whatever the current value of this is. So the very first time my page renders, if we look at our console, just refresh our page here, you can see that it says Kyle, and then we're doing a re-render.

Now if I give my button some text so we can actually see what it's looking like. So I can say change ref. There we go. So now let's inspect our page. Go over to the console, clear everything out, give it a quick refresh. You can see it says Kyle, then re-render. And if I click change ref, you'll notice it's not actually doing anything. Or at least it doesn't look like it's doing anything. But that's because when I change my ref, it doesn't re-render my component. But it is actually changing the value of what this is.

If I create another button that'll just print out our ref, so on click, I'm just gonna do console.log kile ref dot current. All that this function is going to do is print our ref. We can even get rid of this log up here completely. Now if I inspect our page, just refresh everything. You can see it's re-rendering one time. It's printing twice because of strict mode, but it's only rendering once. And I can change my ref a bunch of times, nothing's changing, but when I print my ref,

You can see it's got a brand new value. I click change in print, it's got a brand new value. So I'm able to actually store and save information in my component that has nothing to do with the re-render cycle of React. Now you may think, what is the purpose of doing this? I could just create a variable out here, call it, you know, Kyle, for example, set it to the string Kyle. And that should work just fine. And then I can change it down here, like I was doing. So I could just say ⁓ Kyle is equal to this and print out the value of Kyle.

And that may look like it's going to work, but the problem is this variable is not tied to your component in any way. So that means that it's global to your entire React application. So if I have this app component being rendered seven times in my application, they all share the exact same Kyle variable. So if I change it in one of them, it changes it for all of them. And for 99.9% of use cases, that is not something you want to do. Instead, you want this reference to be tied to your component, but you want it to not be tied to the re-rendering of your component.

Sneha Mehra (00:04:33)  
And that's what a ref is for because if I use a ref like I've done here, now it doesn't matter how many app components I have, each one has their own reference to this Kyle ref. And the value of it is going to change on its own inside of those individual components. So if I change it in one app component, none of the other ones are affected by those changes. So one of the main use cases for use ref is storing data between renders that doesn't actually cause your component to re-render when it changes. But there's a second use case for use ref.

Let's get of all these buttons that we have down here. We don't really care about any of them. And let's actually change this to an input ref and we'll give it no default value at all. And we can get rid of this use effect as well. And the reason I'm calling this an input ref is because refs can also be used to get a reference to an element in HTML. That was actually the original use case for refs when they were first introduced. So every single element inside of React is going to have a ref prop. And you can just set it to a ref. So we can set it to our input ref here. And now what's going to happen is the actual value of

Input ref.current is going to be set to this input element right here. So we can just use a simple use effect. And I can just console.log input ref.current just like that. And now if I just save my page and inspect over here, go over to the console, you can see we have that input which has no value at all. So it's actually giving us a reference to that input. And this is really nice because we could do something like get the value of that input and log it out if I wanted to. So now I could say that every time.

That my name changes, I'm just going to log out the value of that input. So let's refresh our page, inspect. And as you can see, by default, it's empty. If I type something, you can see now it's typing out that value because it's getting the value from the actual input ref itself. This is incredibly useful. Obviously, you wouldn't do something exactly like this, but this is great for if you wanted to come in here and say input ref dot current.focus and like actually focus on that input when the page first renders. So now when I refresh my page, you see it immediately.

Focuses me inside that input because I'm setting this input focus right here. The only important thing to understand about these input refs is that they are actually not available until after your component renders. That's why I'm putting this inside a use effect because a use effect runs after the component stuff is rendered out to the HTML. Essentially, after my HTML is rendered, then my use effect runs. If I actually remove this and save, I'm going to get an error. If I inspect my page, it's just going to say like undefined is not able to be accessed. Can I read properties of undefined? And the reason for that is because my input ref.

Sneha Mehra (00:06:53)  
Currently has no value, so input ref.current is undefined, and it doesn't have a value until after the first time my component is rendered, which is why again I wrap it inside of a use effect if I want to access it on the first time my page renders. Now this isn't going to be a hook that you reach for all the time like state and effect, but it's incredibly useful if you need access to an HTML element or if you need to store something that doesn't cause your component to re-render.

Now in the next video, I'm going to show you how you can actually handle refs inside of class components because it's quite a bit different since there is no use ref hook in class components. So if you're not interested, just skip the next video. Otherwise, I'm going to see you there where we talk about refs in class components.

—------------------------  
Sneha Mehra (00:00:00)  
As you saw in our last video, building out a form, even just a simple form, actually requires quite a bit of work on your end. And as your form starts to grow, get more complex, and have more difficult validations, it can become incredibly difficult to make easy to read, clean, manageable code, which is why most people use some type of form library inside of React when they're writing out their code. Now, currently, when I'm recording this video, there are about three form libraries that are the most popular.

But definitely one that stands above the rest and is what I'm going to be using in this video. So if we look at those three, we have React Hook Form, Formic, and React Final Form. Now, React Final Form, the last version came out a year ago and it only has about 353,000 weekly downloads. So it's by far the least popular of these options and is also fairly outdated. Formic is even more outdated with the last version being published two years ago. And as you can see, it has 2.3 million weekly downloads, so it is quite a popular library.

But the fact that it hasn't been updated in two years is a bit of a downside, especially because the issue numbers keep piling up. There's currently 642 open issues, and that number is only going to become larger as time goes on. Now, React Hook form, you can see the last updated version was seven days ago at the time that I'm recording this. It only has four open issues and is the most popular with 3.1 million downloads. So this is currently the top form library inside of React. It's been that way for quite a while.

And I think it'll continue to be that way unless a new form library comes and overtakes it. Now, the important thing to note about this video and this course overall is that when I start to dive into libraries like this hook form library, you're gonna notice that certain things are gonna become outdated and they're not going to be fully up to date with what's in this video because their libraries add a newer version and so on. And that's because it's really, really difficult to keep up with all the changes to all the libraries out there because they change rather quickly. So instead, what I'm going to do for all the videos where I use a library.

I'm gonna make sure I tell you exactly what version I'm using so you can follow along with that version if you want to to make sure everything works correctly. And also I'm going to make sure to teach it in a way that is hopefully easy for you to learn the new changes if I don't get around to making an update video in time for when you're watching this. So if currently, right now this is using version what 7.4, 3.2, if in the future we are on version 8 or 9 or something like that, hopefully you're going to be able to just make the changes that you need to the code to be able to be updated to that version.

Sneha Mehra (00:02:22)  
Or I'll come out with an updated video telling you what those changes are going to be. But again, I'm not going to dive super deep into every detail of these different libraries. And instead, what I'm going to do is give you the high-level overview so that it's going to be easy for you to apply those to whatever version of the library you use, even if in the future a new library comes along and overtakes this library. The concepts overall are what matters. And the concept here is that using a form library makes managing your forms much easier.

So, with that said, I'm just gonna kind of dive through some of the documentation on React Hook Form to really kind of explain to you what's going on. And in the next video, I'm actually going to show you a project of me using this library to show you how it can work. So, if we go over the getting started documentation for this, we're currently on version 7\. If you're on a future version, your docs are obviously going to look slightly different, but the overall concept should be about the same. The main thing about this React Hook Form library is it has essentially a single hook that you're going to use for 90% of use cases called useForm, and this useForm hook returns an object.

That contains a ton of information. In our case, the main things we're looking at here are register and handle submit. And this register just allows you to hook up all the different event listeners you need for an input. So as you can see here, we have an input. We're calling register, passing it the name of our input, which in our case we just called example, and we're just spreading that out. So it's giving us all the information we need. This is going to give us like on changes, values, blurs, and so on. All those different events that we need are going to be right inside of here.

Also, you can pass along things like, I want to make sure this is a required property, so I'm passing along required of true. Handle submit is just a function that you call and you actually pass it another function. So in our case, we're passing it our on submit function. And all this does is it takes that function and it calls that function and it passes along all of the data from your form. So in our case, it passes along all the data for our example and our example required field right here. We have some other things inside of here that are important, but we're going to cover those as we go through the docs. We scroll down a little bit further.

Here's another example of really simply just doing registering. So as you can see here, we're registering an input and a select. They both work exactly the same, which is really nice. Everything is really easy to use. And again, you see that we have that register and handle submit function. So register just takes the name of your input, whatever you want it to be, and then it just hooks everything up. So for example, our data, if we typed in data.firstname, that'll give us this input value right here. Data.gender is going to give us the value for our select option right here. React hook form also makes it so that validation is incredibly easy.

Sneha Mehra (00:04:38)  
As you can see, we have some validation for HTML, standard validation. We can pass those along right here. So for example, I can say required is true, max length 20\. These are all your standard HTML validation that you can do directly on an HTML input. Another thing that's really important, if we just scroll down here a little ways, is if you need to integrate with things that are like inside of a UI library or some type of you know UI component, like a React Select component, you need to obviously be able to integrate with that directly. And that is where a controller comes in. It allows you to just have some more fine-tuned controls over how everything works.

They have a hook version called use controller or this actual controller component that you can wrap things in, depending on what you need to do. So if we come down here, you can see this is where we integrate controlled inputs. And you can see we're just wrapping those inputs inside this controller here. And it just essentially gives you the option to have a controlled input, which is really useful. This is something I'm going to demonstrate in the next project because I'm going to be using a React Select and showing you how you can actually use a controller and actually control that input because it's separate from the normal ⁓ input that you get with HTML.

We jump forward a little ways. You can see that we have error handling here. As you can see, we get some state from our form, which includes these errors. So if we wanted to be able to render out our errors, you can see it's just going to give us different errors. So it's going to say error.firstname for our first name property. Here it's going to come down here, error.mail. It just matches whatever you register to. So it's really nice that we can actually have that information directly inside of our errors object. And we don't need to worry about anything at all complex about handling our own errors because all of that is handled automatically for us.

And this library also integrates with different schema validation, so like Zod or Yup. In our case, Zod is the one I would recommend using. It's the most modern and up-to-date one. But when you're watching this video, obviously that may be different. But you can integrate these different schema validation libraries through these different resolvers that they have. And you can essentially just hook that directly in right here with your resolver. So you'd have a Zod resolver if you did Zod, for example. That just gives you even more in-depth validation.

So this video is mostly meant to just be a quick overview of kind of what the React hook form can do. As you can see, it's relatively easy to set up. You just have this single use form right here that you can pass along any information that you want to. And then it gives you all these different functions that you can use or different data points that you can use to make your form exactly like you want it to. So it essentially takes all the boilerplate related to creating a form and removes it, which is really nice. And in the next video, like I mentioned, I'm going to show you how it implement this inside of a specific form.

—--------------------------

Sneha Mehra (00:00:00)  
As you saw in our last video, building out a form, even just a simple form, actually requires quite a bit of work on your end. And as your form starts to grow, get more complex, and have more difficult validations, it can become incredibly difficult to make easy to read, clean, manageable code, which is why most people use some type of form library inside of React when they're writing out their code. Now, currently, when I'm recording this video, there are about three form libraries that are the most popular.

But definitely one that stands above the rest and is what I'm going to be using in this video. So if we look at those three, we have React Hook Form, Formic, and React Final Form. Now, React Final Form, the last version came out a year ago and it only has about 353,000 weekly downloads. So it's by far the least popular of these options and is also fairly outdated. Formic is even more outdated with the last version being published two years ago. And as you can see, it has 2.3 million weekly downloads, so it is quite a popular library.

But the fact that it hasn't been updated in two years is a bit of a downside, especially because the issue numbers keep piling up. There's currently 642 open issues, and that number is only going to become larger as time goes on. Now, React Hook form, you can see the last updated version was seven days ago at the time that I'm recording this. It only has four open issues and is the most popular with 3.1 million downloads. So this is currently the top form library inside of React. It's been that way for quite a while.

And I think it'll continue to be that way unless a new form library comes and overtakes it. Now, the important thing to note about this video and this course overall is that when I start to dive into libraries like this hook form library, you're gonna notice that certain things are gonna become outdated and they're not going to be fully up to date with what's in this video because their libraries add a newer version and so on. And that's because it's really, really difficult to keep up with all the changes to all the libraries out there because they change rather quickly. So instead, what I'm going to do for all the videos where I use a library.

I'm gonna make sure I tell you exactly what version I'm using so you can follow along with that version if you want to to make sure everything works correctly. And also I'm going to make sure to teach it in a way that is hopefully easy for you to learn the new changes if I don't get around to making an update video in time for when you're watching this. So if currently, right now this is using version what 7.4, 3.2, if in the future we are on version 8 or 9 or something like that, hopefully you're going to be able to just make the changes that you need to the code to be able to be updated to that version.

Sneha Mehra (00:02:22)  
Or I'll come out with an updated video telling you what those changes are going to be. But again, I'm not going to dive super deep into every detail of these different libraries. And instead, what I'm going to do is give you the high-level overview so that it's going to be easy for you to apply those to whatever version of the library you use, even if in the future a new library comes along and overtakes this library. The concepts overall are what matters. And the concept here is that using a form library makes managing your forms much easier.

So, with that said, I'm just gonna kind of dive through some of the documentation on React Hook Form to really kind of explain to you what's going on. And in the next video, I'm actually going to show you a project of me using this library to show you how it can work. So, if we go over the getting started documentation for this, we're currently on version 7\. If you're on a future version, your docs are obviously going to look slightly different, but the overall concept should be about the same. The main thing about this React Hook Form library is it has essentially a single hook that you're going to use for 90% of use cases called useForm, and this useForm hook returns an object.

That contains a ton of information. In our case, the main things we're looking at here are register and handle submit. And this register just allows you to hook up all the different event listeners you need for an input. So as you can see here, we have an input. We're calling register, passing it the name of our input, which in our case we just called example, and we're just spreading that out. So it's giving us all the information we need. This is going to give us like on changes, values, blurs, and so on. All those different events that we need are going to be right inside of here.

Also, you can pass along things like, I want to make sure this is a required property, so I'm passing along required of true. Handle submit is just a function that you call and you actually pass it another function. So in our case, we're passing it our on submit function. And all this does is it takes that function and it calls that function and it passes along all of the data from your form. So in our case, it passes along all the data for our example and our example required field right here. We have some other things inside of here that are important, but we're going to cover those as we go through the docs. We scroll down a little bit further.

Here's another example of really simply just doing registering. So as you can see here, we're registering an input and a select. They both work exactly the same, which is really nice. Everything is really easy to use. And again, you see that we have that register and handle submit function. So register just takes the name of your input, whatever you want it to be, and then it just hooks everything up. So for example, our data, if we typed in data.firstname, that'll give us this input value right here. Data.gender is going to give us the value for our select option right here. React hook form also makes it so that validation is incredibly easy.

Sneha Mehra (00:04:38)  
As you can see, we have some validation for HTML, standard validation. We can pass those along right here. So for example, I can say required is true, max length 20\. These are all your standard HTML validation that you can do directly on an HTML input. Another thing that's really important, if we just scroll down here a little ways, is if you need to integrate with things that are like inside of a UI library or some type of you know UI component, like a React Select component, you need to obviously be able to integrate with that directly. And that is where a controller comes in. It allows you to just have some more fine-tuned controls over how everything works.

They have a hook version called use controller or this actual controller component that you can wrap things in, depending on what you need to do. So if we come down here, you can see this is where we integrate controlled inputs. And you can see we're just wrapping those inputs inside this controller here. And it just essentially gives you the option to have a controlled input, which is really useful. This is something I'm going to demonstrate in the next project because I'm going to be using a React Select and showing you how you can actually use a controller and actually control that input because it's separate from the normal ⁓ input that you get with HTML.

We jump forward a little ways. You can see that we have error handling here. As you can see, we get some state from our form, which includes these errors. So if we wanted to be able to render out our errors, you can see it's just going to give us different errors. So it's going to say error.firstname for our first name property. Here it's going to come down here, error.mail. It just matches whatever you register to. So it's really nice that we can actually have that information directly inside of our errors object. And we don't need to worry about anything at all complex about handling our own errors because all of that is handled automatically for us.

And this library also integrates with different schema validation, so like Zod or Yup. In our case, Zod is the one I would recommend using. It's the most modern and up-to-date one. But when you're watching this video, obviously that may be different. But you can integrate these different schema validation libraries through these different resolvers that they have. And you can essentially just hook that directly in right here with your resolver. So you'd have a Zod resolver if you did Zod, for example. That just gives you even more in-depth validation.

So this video is mostly meant to just be a quick overview of kind of what the React hook form can do. As you can see, it's relatively easy to set up. You just have this single use form right here that you can pass along any information that you want to. And then it gives you all these different functions that you can use or different data points that you can use to make your form exactly like you want it to. So it essentially takes all the boilerplate related to creating a form and removes it, which is really nice. And in the next video, like I mentioned, I'm going to show you how it implement this inside of a specific form.

—----------------------------------

Sneha Mehra (00:00:00)  
As you saw in our last video, building out a form, even just a simple form, actually requires quite a bit of work on your end. And as your form starts to grow, get more complex, and have more difficult validations, it can become incredibly difficult to make easy to read, clean, manageable code, which is why most people use some type of form library inside of React when they're writing out their code. Now, currently, when I'm recording this video, there are about three form libraries that are the most popular.

But definitely one that stands above the rest and is what I'm going to be using in this video. So if we look at those three, we have React Hook Form, Formic, and React Final Form. Now, React Final Form, the last version came out a year ago and it only has about 353,000 weekly downloads. So it's by far the least popular of these options and is also fairly outdated. Formic is even more outdated with the last version being published two years ago. And as you can see, it has 2.3 million weekly downloads, so it is quite a popular library.

But the fact that it hasn't been updated in two years is a bit of a downside, especially because the issue numbers keep piling up. There's currently 642 open issues, and that number is only going to become larger as time goes on. Now, React Hook form, you can see the last updated version was seven days ago at the time that I'm recording this. It only has four open issues and is the most popular with 3.1 million downloads. So this is currently the top form library inside of React. It's been that way for quite a while.

And I think it'll continue to be that way unless a new form library comes and overtakes it. Now, the important thing to note about this video and this course overall is that when I start to dive into libraries like this hook form library, you're gonna notice that certain things are gonna become outdated and they're not going to be fully up to date with what's in this video because their libraries add a newer version and so on. And that's because it's really, really difficult to keep up with all the changes to all the libraries out there because they change rather quickly. So instead, what I'm going to do for all the videos where I use a library.

I'm gonna make sure I tell you exactly what version I'm using so you can follow along with that version if you want to to make sure everything works correctly. And also I'm going to make sure to teach it in a way that is hopefully easy for you to learn the new changes if I don't get around to making an update video in time for when you're watching this. So if currently, right now this is using version what 7.4, 3.2, if in the future we are on version 8 or 9 or something like that, hopefully you're going to be able to just make the changes that you need to the code to be able to be updated to that version.

Sneha Mehra (00:02:22)  
Or I'll come out with an updated video telling you what those changes are going to be. But again, I'm not going to dive super deep into every detail of these different libraries. And instead, what I'm going to do is give you the high-level overview so that it's going to be easy for you to apply those to whatever version of the library you use, even if in the future a new library comes along and overtakes this library. The concepts overall are what matters. And the concept here is that using a form library makes managing your forms much easier.

So, with that said, I'm just gonna kind of dive through some of the documentation on React Hook Form to really kind of explain to you what's going on. And in the next video, I'm actually going to show you a project of me using this library to show you how it can work. So, if we go over the getting started documentation for this, we're currently on version 7\. If you're on a future version, your docs are obviously going to look slightly different, but the overall concept should be about the same. The main thing about this React Hook Form library is it has essentially a single hook that you're going to use for 90% of use cases called useForm, and this useForm hook returns an object.

That contains a ton of information. In our case, the main things we're looking at here are register and handle submit. And this register just allows you to hook up all the different event listeners you need for an input. So as you can see here, we have an input. We're calling register, passing it the name of our input, which in our case we just called example, and we're just spreading that out. So it's giving us all the information we need. This is going to give us like on changes, values, blurs, and so on. All those different events that we need are going to be right inside of here.

Also, you can pass along things like, I want to make sure this is a required property, so I'm passing along required of true. Handle submit is just a function that you call and you actually pass it another function. So in our case, we're passing it our on submit function. And all this does is it takes that function and it calls that function and it passes along all of the data from your form. So in our case, it passes along all the data for our example and our example required field right here. We have some other things inside of here that are important, but we're going to cover those as we go through the docs. We scroll down a little bit further.

Here's another example of really simply just doing registering. So as you can see here, we're registering an input and a select. They both work exactly the same, which is really nice. Everything is really easy to use. And again, you see that we have that register and handle submit function. So register just takes the name of your input, whatever you want it to be, and then it just hooks everything up. So for example, our data, if we typed in data.firstname, that'll give us this input value right here. Data.gender is going to give us the value for our select option right here. React hook form also makes it so that validation is incredibly easy.

Sneha Mehra (00:04:38)  
As you can see, we have some validation for HTML, standard validation. We can pass those along right here. So for example, I can say required is true, max length 20\. These are all your standard HTML validation that you can do directly on an HTML input. Another thing that's really important, if we just scroll down here a little ways, is if you need to integrate with things that are like inside of a UI library or some type of you know UI component, like a React Select component, you need to obviously be able to integrate with that directly. And that is where a controller comes in. It allows you to just have some more fine-tuned controls over how everything works.

They have a hook version called use controller or this actual controller component that you can wrap things in, depending on what you need to do. So if we come down here, you can see this is where we integrate controlled inputs. And you can see we're just wrapping those inputs inside this controller here. And it just essentially gives you the option to have a controlled input, which is really useful. This is something I'm going to demonstrate in the next project because I'm going to be using a React Select and showing you how you can actually use a controller and actually control that input because it's separate from the normal ⁓ input that you get with HTML.

We jump forward a little ways. You can see that we have error handling here. As you can see, we get some state from our form, which includes these errors. So if we wanted to be able to render out our errors, you can see it's just going to give us different errors. So it's going to say error.firstname for our first name property. Here it's going to come down here, error.mail. It just matches whatever you register to. So it's really nice that we can actually have that information directly inside of our errors object. And we don't need to worry about anything at all complex about handling our own errors because all of that is handled automatically for us.

And this library also integrates with different schema validation, so like Zod or Yup. In our case, Zod is the one I would recommend using. It's the most modern and up-to-date one. But when you're watching this video, obviously that may be different. But you can integrate these different schema validation libraries through these different resolvers that they have. And you can essentially just hook that directly in right here with your resolver. So you'd have a Zod resolver if you did Zod, for example. That just gives you even more in-depth validation.

So this video is mostly meant to just be a quick overview of kind of what the React hook form can do. As you can see, it's relatively easy to set up. You just have this single use form right here that you can pass along any information that you want to. And then it gives you all these different functions that you can use or different data points that you can use to make your form exactly like you want it to. So it essentially takes all the boilerplate related to creating a form and removes it, which is really nice. And in the next video, like I mentioned, I'm going to show you how it implement this inside of a specific form.

—------------------------

Sneha Mehra (00:00:00)  
Up to this point, we've covered a lot of the more basic features of components, such as state lifecycle methods, and just how components work in general. And now in this section of the course, I want to cover some more advanced topics related to components. This is going to be kind of a grab bag section where there's a lot of different kind of unrelated topics thrown in that are a little bit more advanced but don't really have a good place to fit in the rest of the course. The very first thing I want to talk about is the dev tools because as you start covering more advanced topics, chances are you're going to mess certain things up, and understanding how the dev tools works makes it much easier to actually fix these bugs.

Now for the code that we're going to be working with, this is very similar to the code that we've used before for our different components for that name and counter example. As you can see here, this is almost exactly the same code we had before, but I have a few additional things inside of here. So if we go to our app, you can see we're rendering out both the class and function version. So we can see how those work inside the dev tools differently. And then inside of both versions, I just have a simple function here for component.update that updates our document title. And I have the exact same thing in our function. I'm just using a use effect to do that update instead.

Then the final thing you can see here is this display string down here where I say my name is and I'm zero years old. That is going to be inside of its own component. So as you can see here, it just says own component where you pass in the name and the age. And that's just so I can show you how different nesting things work as well as how passing around props work inside of the DevTools. So let's say you're going along and you want to actually inspect using the dev tools what's going on. Well, you're going to need to use the React DevTools, which I mentioned to download at the beginning of this course. So if you haven't already done that, I recommend doing that now.

So once you get your dev tools up in here, what you're gonna wanna do is you're wanna go over to the tab where it says components, and you can see it has this little React icon next to it. Once you open that up, it's going to give you all the information for our components. I'm gonna zoom this out a little bit so we can see everything side by side, kind of how it would normally look on your screen. And you can see over here on the left hand side, we have all the different information for the different components in our application. So we have our app component, and as you can see, when I highlight over these things, on the right hand side of my screen, it highlights what each component entails. Then we have our class component, which is the top section. Inside that, we have the display string.

And then we also have our function component, which has its own display string inside of it. And if I click on these different elements, right now I'm on the app, you can see it tells me things such as the props, what different components is rendering this, as well as the source for where this is actually happening inside of my code. So let's say I click on this function component for now. Actually, let's do class for now. You can see inside the props it's empty. I can add new props if I want. I could say like custom prop, and I could give it a value of whatever I want. Let's just say three.

Sneha Mehra (00:02:17)  
Just like that, that's actually going to pass that along as a prop, as you can see there. So you can add in and change the different props of what's happening. So for example, I could change this to 34 if I wanted. But our component by default, if I refresh the page, does not have any props at all. So it's just an empty list. Next up, you can see that we have the state and it shows all of our state variables. Age is zero, name is blank. And if I change this to, for example, four and I you know exit, you can see that it updated in my application to have that value of four. So I can manually change my state and see how my application responds to that.

Then down here we have that rendered by section again. So you can see that it is rendered inside of our app, which is rendered inside of the create root as well as the React DOM. These bottom two don't matter as much, but as you can see, I can click on the app and navigate directly to that. And then at the bottom we have the source for where that code is again. Now if I jump into the display string here, you can see that this actually has props. So if I change the prop here, for example, of age to seven and I change go back here, you can now see that that is updated inside here. So one thing to note about changing around your different values is you notice.

That in my application, I'm passing along my age from this you know plus minus up here into my props. But by manually changing my props, I'm kind of overriding that. So just make sure that when you manually change things, you know you know what you're doing. Because now when I change this, it's obviously going to reset back to what it was. But if I manually set this, as you can see, it it comes out of sync essentially. So just make sure you understand what you're doing and that things are not always going to sync up perfectly if you're manually changing props or state. And again, down here I have that rendered by section, and there's no state, so there's nothing showing up in the states.

Now if I look at my function component, you're gonna notice it's a little bit different.

Instead of having a state section, we have a hook section, which shows you all the different hooks inside of your application, which is really important. And you'll notice right now that state doesn't actually have any names for anything. You can get around this by clicking on this little magic icon. As you can see, it says parse hook names, and it may be slow because it has to parse through all of your actual code. But when you click on that, you can now see that this is the name and this is the age. And it just takes whatever name you used for the variable inside your code and uses that as the actual name for the state. So you can we have our name and we have our age. And again, I can update this to like six. Now you can see it updated to six in my application.

Sneha Mehra (00:04:11)  
So that's the super bare bones basic stuff that you're going to do with the DevTools and it honestly covers 90% of what you're going to run into. But there's some other things I want to talk about. First of all, over here you have this little selector. And when you click on this, what you can do is you can highlight different elements on your page and actually select any of them. So I can come up here and select this display string. And now it's going to bring me to that exact component on the left-hand side inside my dev tools. So it's a great way to actually see where your dev tools are. You can just click on this and say, I want to inspect this thing, and it's going to bring you exactly to where that component is.

Now also we can search. So I could, you know, search search for class and you can see it's going to find exactly where that is inside my list. Makes it easy when you have a lot of components showing up. Before we jump into the settings, I want to come over to this section over here. If you're dealing with suspense in React, which is a fairly advanced concept, you can click on this and it'll suspend your component for you. This is only for dealing with like asynchronous React stuff. It's covered way later in this course. So don't worry about it right now. Just know that this is there. So if you want to make sure you do that asynchronous stuff again to see what happens, you can click that button.

Next here, you can actually click this and it'll bring you to the elements tab where that actual element is. So as you can see, right here, that div, that is where our component is. So it's just bringing us over to that section. Let's go back to our components here. ⁓ The other thing we have over here is logging out this component to the console. So if I click on this and go to the console and I expand it, you can see I get all the information on my component. So you can see have my props, my state, all the different things that are being rendered out, as well as where this code is inside of my file system and so on.

So it just gives you a ton of information about your component, all the different things inside of it. So it may be useful if you want to inspect things in the DOM you can or inside the console, you can do that. And then finally, this button right here just brings you to the source for where your actual code is. So as you can see, here's the file that contains all of my code for that actual component. Now jumping over into the settings, if we click on this, there's a few different things you can do. We'll start with general tabs for now. Most of this is just around actually how you want this to look, whether or not you want things to be compact, which is the default, or comfortable, which is what I have set so it's a little bit bigger and easier to read.

You can also make it so it highlights the components when they are re-rendered. This is very similar to how Google Chrome already highlights things when they change inside of the elements tab. So if you want, you can have that on or off. And then of course you can change your theme, you know, if like a dark mode theme or light mode, whatever you want. Next in the debugging tab, as you can see here, we have a few settings. I already talked about this hide logs during second render and strict mode. I already talked a bit about that, but there's some other settings such as if you want to break on when you have warnings, you want to show your different warnings, and so on. So you can jump into here to really see what's going on. The most important one is probably this components tab though.

Sneha Mehra (00:06:28)  
So if you wanted all of your hook names to automatically be parsed, you can just select this checkbox and you don't need to click that little magic icon, it'll do it automatically for you. But if you have really big files or a very large project, this may slow down your code a little bit. So I just leave it unchecked and I only parse the names when I need them because it just takes one single button click. This just expands or collapses your tree by default when you open up the page. Super self-explanatory. And then finally, down here we have a filter that allows us to hide and show different things inside of our list. So by default, it hides all of the different DOM nodes.

If I actually remove this and I come back over here, you can now see every single DOM node, like our div and our inputs and our you know line breaks and so on. These are all showing up inside of the tree. This is something that you may want or you may not want. Honestly, I find that it's a little bit overload because I really don't need all this information, which is why the default is having that in place. And I like to keep that in place. So we'll just keep that there. As you can see, we can have things like location, the name for the component, whether or not it's a higher order component, which is again something we cover later in this course.

Or based on different types, like if it's a class component, a function component, so on. In our case, we're just gonna hide all the DOM nodes, which is the default, which I like to do. Lastly, you have the profiler here. We're not gonna be covering the profiler in this video, but there's settings right here for covering the profile. And this is something I'm gonna do much later in the course when we start talking about performance. At this point in the course, it really doesn't make sense to go over profiling because we haven't even covered enough things about performance to even you know have a benefit from doing all this different profiling.

Speaking of the profiler though, if you go over here, you can see that we have the profiler tab. This is where you would do all the profiling that you want. And this just brings up the same exact settings menu. So once you dive into profiling, this is where it's going to be done. But most of the dev tools that you're going to be doing is probably going to be in this components tab right here. And just being able to click through everything and see exactly what's happening with all of your different hooks, all of your different state, all of your different props, what's rendering what, and so on, that's where this is really important.

—------------------

Sneha Mehra (00:00:00)  
So let's go ahead and actually implement these things step by step. So the very first thing I need to do is run npmi and npm rundev. I've already done that, so I have the project showing up on the right hand side of my screen. And then what we can do is we can move on to these instructions. So the first thing I want to do is I want to log out the text render each time my component re-renders. That's relatively easy. So we can just say ⁓ use effect. And inside of here, I want this to be every time my component re-renders. So I just pass nothing to my use effect as the second parameter. And I can just console.log the text.

re-render just like that. So now if I just give my page a quick inspect, every single time something changes, you can see that we're logging out re-render and even when we are mounting our component. So already we're off to a good start. That first check mark is done. Secondly I want to log out the text high whenever my component mounts. So let's just copy this use effect and here we want to log out the text high. ⁓ But I only want to do this on mount. So if I pass it an empty dependency array, this is only going to run when I mount my component. Now if we whoops

inspect our page here. We go to the console and I refresh. You can see it's printing out re-render because that runs every time it renders and we can see we get high being printed. And no matter how many times I re-render my component, hi never gets reprinted until I go ahead and remount my component by showing it and hiding it. So this again is just to emulate what remounting will do. So we got the high one done. Let's move on to the next step which is to log out my name is and I am age years old whenever name or age changes. So I'll just copy that string, move over here.

Use effect ⁓ console.log, and I want to log out whoops that string right there. So it's going to be name and age. So every single time my name ⁓ or my age changes, I want to make sure that this gets logged out. So if I just inspect my page again, go to the console. Every time I'm changing my name, you can see this is getting printed. And every time I change my age, you can see it's also being printed. So again, working as we would expect.

And that right there is all of them except for our last one here for updating the document title whenever our name changes. This one hopefully was pretty easy because we did exactly this in the previous video. So we can just say use effect ⁓ anytime our name changes. I want to update our document dot title to be equal to our name. So now if we change our name, you can see it's being rendered up top in our document title. Another important thing to note, just kind of a side note here about use effect.

Sneha Mehra (00:02:26)  
Is anytime you use a state variable inside your user effect or just any variable in general, it doesn't matter what it is, if the variable can change, you should include it inside of your dependency arrays. That's just something really important to note is that if you have a variable that can change, make sure you put it inside your dependency array. Because it's really easy to forget to add something like name inside of here, and then obviously this won't work anymore. As you can see, our document title is not changing. Now, if we go back to our README, we can move on to the bonuses where it gets a little bit more confusing. This first one is quite a bit easier. We need to figure out.

How does a use effect work on unmount? Well, I mentioned, if we go over here, every time our component unmounts, it calls ⁓ this return function here. And I also mentioned that this return function is called every single time our use effect is re-ran. Well, if we only want it to ever be called on unmount, what we need to do is we need to add this return to our use effect with no dependencies inside of it. ⁓ So if I put this inside of here, we know this use effect will never run twice, which means this return never gets called on re-renders.

But it always gets called on unmount, so this will only run when our component unmounts. ⁓ So if I console log by inside of here, and I just come over to our page and I inspect, look at our console, clear everything out, which do a refresh. You can see it renders out high and all the other stuff. And it doesn't matter what I change. You can see I'm changing a bunch of stuff. Nowhere in here does it render out by, but as soon as I hide this, you can see it prints out the text by, which of course I spelled wrong. There we go. You can see it prints that text out though every single time I get to that point.

And that's because I'm unmounting the component. And if I had other return statements, those would run as well. But this is the only one I care about because I only want this to run every single time I unmount my component and no other time at all. Now if we move on to the last step of our README, this is the most confusing. Essentially I said one second timer that I talked about on our name change. So if we go into here, we could add this into our use effect here with our name, or we could create a brand new use effect that runs off of the name. Either way is fine. It really depends on your preferences.

Generally for the most part, I like to break my use effects out if they're unrelated from each other into their own sections, but you can also add them together if you want. In our case, I'll put them in one use effect just for simplicity purposes. So what we need to do here is we need to set up a timeout. So we can do set timeout just like this. And this timeout is going to log that out. So what did I say it should log out? It should log out. My name is and whatever the name is. So console.log, my name is, just like that. And I want this to happen after one second.

Sneha Mehra (00:04:51)  
So, this is probably your first thought. This is probably what you did immediately when you saw this. But if we inspect our page, I'm actually going to comment out all the rest of this code so we can really just focus on the one log that we care about. We refresh our page and inspect. You'll notice that when we change our name, I'm just going to do ASDF. You'll see after a second of each key press it'll print out my name is A, A S, A S D, A S D F. And that's not what we want. Essentially, what we want to do is we want to be able to type a bunch of stuff. And as soon as we have a one second delay in our typing,

we then want it to print out to the console. We don't want it to print every single time. So instead we need to actually clean up this use effect by removing our timeout if this timeout has not run yet. So we can just say const timeout equals, and then down here we can do a simple return.

And then a return we can just clear our timeout. So essentially what I'm doing here is I'm running my timeout. And if we have a one second or greater delay, the code inside of here will run just fine. But if we change our name before this timeout runs, well, we're clearing it, which means it'll never run at all. It just immediately stops and can never run. So to see exactly how this works, let's inspect our page and we'll just start typing stuff. And you'll notice I'm typing without any one second delay, and now I pause. After one second, you can see it prints out what the current name is. And if I

Do a long pause between each key press. You can see it prints out each individual one. But if I have a bunch of key presses with no delay and then release, you can see it'll print out the full thing at after one second of changes. Now this again was probably the most confusing section. And if you didn't get this, don't feel bad at all. I purposely didn't cover this to really push you, but if you didn't get it, that's 100% okay. The bonus sections are not expected to be got on your first try. And if you had to watch this video again, don't feel bad at all.

Now if we comment the rest of this code back in and save, this is the complete version. Every single thing inside of our README has been covered, and this is what your code should generally look like. Again, if your code's not exactly the same, it's fine, but for the most part your code should probably look pretty much identical to mine because this is such a simple project, there's not really many other ways to do it than what I've done here.

—-------------------------  
Sneha Mehra (00:00:00)  
So in the last video, we talked all about the use state hook and how you can use that in function components. But unfortunately, you cannot use the use state hook or any other React hook in a class-based component. So we need to talk about how state actually works in class-based components because it's slightly different than it does in function-based components. So what I'm gonna do is I'm gonna create a brand file. I'm gonna call it appclass.jsx. And inside my main here, I just want to render out that app class file instead of my original app. So if we come over here, we can say export.

Default ⁓ class ⁓ app class, ⁓ just like that. And now that's going to give us access to that. And we can do our simple render. ⁓ And I could just say return ⁓ h1 that says hi. And now if we refresh and we make sure that we ⁓ extend react.component. There we go. We'll just make sure we import React from ⁓ React. There we go. So now you can see it's printing out the text high. And it's coming from this class component specifically.

So if we go over to our function component, you know that we use this use state hook to start hooking in and dealing with state, but we don't have that in class components. So instead we need to use the constructor. So the constructor here is always going to take in some props, and we want to make sure we pass those props to our super method here. So we can pass in our props just like that. Whenever you redefine this constructor, you need to make sure you take in the props and you pass those along by calling super. Then from there, we can set this.state. ⁓

To our state variable. And this is just going to be an object. Unfortunately, with class components, state is always one single thing. It's just called this.state. So we always have to set it to essentially one single object. While in function components, we can break it up into multiple different use states. Class components, everything has to be one thing. So we're going to set this to an object. We're going to say the name is Kyle. We're just going to start out really straightforward. So that is the only thing inside of our state right now. So now inside of our render, we could get that state variable by saying this dot state. ⁓

name. ⁓ And now when I save you can see it prints out Kyle, just like we were using before. Now if we wanted to set up an on click here, ⁓ we can say on click is handle click. And we can make that function.

Sneha Mehra (00:02:10)  
Just like this. And we can make it so that it updates the state. But to update state, what we need to do is call this.set state. And in here, what we need to do is we actually need to pass an object that's going to overwrite what we have. So in our case, we'll say name is Sally. ⁓ Now if I save this and click, you're gonna notice it doesn't actually change my name to Sally. And that's because of JavaScript, not React. It's because of how the this keyword is handled inside of functions and inside of classes, and it's a real huge pain. It was one of the biggest downfalls of using class components.

One easy way to get around this is we could just inline our function. So we could just copy exactly what we have here, paste it into here. And now this will actually work when I click. You can see the name updates to Sally exactly as we would expect. So really the main thing to see here is that instead of using use state and having a variable and a setter, you have one single state, which is called this.state, and you have one single way to update things, which is this.set state. Now if we wanted to do this with our function here instead, we could change this to an arrow function by saying const handle click.

Exals, our arrow function, and then we can pass in handle click here. This will also work because if you click you can see that it's working fine, or we could try binding things. I'm not going to cover binding in this video as it's not at all important unless you're heavily working in class components. But if you want a video on binding, I'm going to link one in the description for you if you want to check it out. Again, I wouldn't worry about it unless you're super heavy into class components. And in that case, the functions like this, the arrow functions, they're going to work just fine as an alternative. So unless you hate arrow functions, this is going to be fine just to use arrow functions here.

Now let's also do the same thing we did before where we had an age. Let's set it to 27\. And let's also print out this.state dot age, just like that. So if I save, you can now see we have our name and our age. But you'll notice something really interesting. When I click, our name updates, but our age does not change. It doesn't get removed. And that's because the way set state works in classes is you pass it the new values you want to update, but it won't change any of the existing values except for the ones that you tell it to update.

This is really nice because everything's in one single state and you don't want to have to redefine the entire state every single time. So this is a nice little quality of life feature. But again, it can be kind of confusing at first glance. Like, why does my age not get removed? And that's because nothing gets removed if you don't put it. It'll only change the values that you add. So if we also wanted to add in a setter for our, you know, count here, we could do that too. So we could come in here and we could say that our age is going to be ⁓ this dot state dot age plus one.

Sneha Mehra (00:04:34)  
And now you can see every time we click, our age goes up by one. But we have the same problem here we had before, where if we try to do this twice, it'll only increment by one at a time. So again, you have access to the current state. And we could do the exact same thing we did before. So we could come in here, I could return this, and I could use our current state.age and add one to it. ⁓ This right here is essentially the same thing where I pass it the function version. The function version gives me the current state, which is essentially this.state.

And it's going to give me my age by doing current state.age. So ⁓ now if I run this, it still increments by one. But if I copy it and paste it down, you'll notice things now increment by two. It's the exact same problem we ran into in the previous video where we were talking about use state. Now I want you to actually try out doing some state on your own. So I'm gonna have you create this exact counter example that we did in the last video. So I want you to create a new component called counter, have the state start at zero, and then every time you click on the counter, I want you to increment it by one.

But obviously, I want you to do this with class components instead of function components. So here's the function version. Try to make a class version as well. So hopefully you pause the video here and try it out yourself. I'll show you what I would do. I'm just going to come in here, I'll say counterclass.jsx. And I'm just going to copy everything we have inside of here. So this is going to be counter class ⁓ extendsreact.component. There we go. And I want this to be a class instead of a function. And I want to import here.

React. So that's the beginnings we have here. We know we have our state, we want to default it to zero. So we need a constructor for that. It takes in our props and we call super with those props. And then this.state is going to be equal to an object. We'll call it counter of zero. Now next ⁓ we need our render function, ⁓ just like this, and that's going to return to us this div. And we also want this handle click up in here as well.

Clean this up a little bit. So our counter here is going to be this.state dot counter. And then when we want to handle our click, we need to make sure this is an arrow function. Otherwise, the this binding is not going to work as we would expect it to. So we're going to set that equal to this arrow function right here. And then of course instead of calling setCounter, we need to call this. We want to return a new state. And this new state is just going to be. ⁓

Sneha Mehra (00:06:57)  
our age our sorry our counter is going to be our current state dot counter plus one there we go that should be everything we need to do to get this counter working so I believe if we come to our app class and inside our render we just render out our counter of the class variety close that off get rid of all this extra code that we don't need none of this we need we're just rendering out a single other component and now when I click this you can see it's incrementing by one each time. So really the big differences here

is we have one state object which contains all the state for our entire component. If you want to use functions inside of our render component, or we just want to use functions anywhere inside of a class component, you either need to use an arrow function or you need to bind it properly. And then when you update the state, you need to use this.setState instead of the individual setters that we have, for example, setCounter here. I guess the only other big difference to think about is the constructor is only ever called one time. So you don't have to worry about setting state with a function versus a non-function like we talked about in

How you only want it to run one time, because in a class-based scenario like we have here, the constructor is only ever going to be run one time, so you don't have to worry about what happens if your set state up here is really slow, it's always going to be run one time, while in the more function-based version, this will run every single time your component re-renders. Other than that, they act exactly the same. Now that's all I want to cover about state.

the use state and class version of state before we jump into our first project, but I do have one other video that's going to be our next video where we talk about how you can actually set up event listeners in a little bit more depth. We've talked about them a little bit with OnClick, but I want to go even more in depth because there's a few nuances you should understand.

—-----------------------------  
Sneha Mehra (00:00:00)  
So the very first thing we need to do is obviously to import and use that hook. So we can say import use local storage from dot slash use local storage. And then we can create that file, use local storage.js, export a function called use localStorage, ⁓ just like that. So now at least we have that file being imported for us. Now we just need to make sure we take in these two values and return these two values.

So the very easy thing we can do here is we can say we have our key ⁓ and we have our initial value. And what I want to do is I just want to set up a state for our value. So we can say value set value is equal to use state. And that use state is just going to take in our initial value for now, just like that. ⁓ And then we can return ⁓ the value and the set value.

So right now all we're doing is we're just emulating how use state works, but that'll at least allow us to have everything working on our screen. But when we refresh, nothing's being stored in local storage. So we need to set that up next. The easiest way to persist things into local storage is going to be with a use effect. We can just say use effect just like this. ⁓ And what we can do is we can say every time our value changes, I want to store this information in our local storage. So what we can do is we can say local storage.setItem. ⁓

With our key, I want to set this particular value. So now if I refresh our page and I inspect over here, go to the console, you can see we're getting an error saying we're rendering more hooks than we did on the previous render. The reason that's happening is because I added this hook and then saved and it's doing some hot module reloading. Just click refresh, it'll fix all the problems. If I go over to the application tab, you can immediately see as I change my first name, it updates in my local storage, which is perfect. But when I refresh, it's not using that value. Instead, it's just resetting it. So instead we need to pull the value from our local storage.

before we actually do anything with our page. So that's where our use state comes in. You know that this takes a function. And this function we can use to query our local storage. So we can say ⁓ the local value is going to be equal to local storage.getItem at that particular key. And if our local value exists, we want to use it. So if it equals null, what we can do is we can do something. And if it doesn't equal null, then we can just return the local value because we know we have that value. So we can just come in here

Sneha Mehra (00:02:15)  
We say if it is equal to null, then we want to use our initial value instead. So what this really is doing is it's first getting our value from local storage and it's saying, hey, if the value does not exist, use our default initial value we pass in. Otherwise, use the value from local storage. So now if I change my value and click refresh, it's actually pulling the value in from local storage. And I can change it and refresh and it's still keeping that same exact value, which is exactly what I want.

So if we really quickly go over to our README, it looks like we've already completed everything's in step one and two of our instruction. It really wasn't too much code, but the actual thought process that goes into making it work is rather confusing, especially if you're not super familiar with how to do things the React specific way. Now we can move on to the bonus section. So the first one is to make sure that we can pass in a function to this instead of just a normal value. So let's uncomment this code for our last name, come down here, comment in the section for our last name as well.

So now we have that last name and immediately we're gonna know that this does not work as we expect. If I try to change the value, refresh, it appears like it's working, but if we just inspect our page and I remove all of our values from local storage and I refresh, it should use our default values. This has a value of default, but you can see it's not being set inside of here at all. We inspect our page even, you may even notice there's an error and there is invalid prop value, it's saying that this value is not correct, and that's because we're not properly calling this function.

So inside of our use local storage hook, this initial value here could be either a function or a value. So if it's a function, we need to call it. So we can say if the type of our initial value is equal to a function, well then what we want to do is we want to return calling that function. So we can say return initial value and we can call it. ⁓ Otherwise, we can just return our initial value because now we know it's not a function, we know it's actually a value. So if I refresh now,

And I make sure that I clear out our local storage again because clearly everything's wrong inside there. And I do a refresh, you can see it's properly getting that default value for us, which is exactly what we want. And best of all, if I change this, change this, refresh, everything is persisting just like I want. And it's only using this initial value when there is no value being stored currently in our local storage. Now the final bonus section we need to deal with is the one for dealing with JSON. So let me just comment these sections in and get rid of those bonus comments. We don't need them.

Sneha Mehra (00:04:34)  
So now we have this ability to add some hobbies, but if I refresh, it's not actually persisting that. And we're actually going to get a bunch of errors because we're not properly serializing things using JSON. So what I need to do is I need to convert all this code to use JSON instead. So this local value right here is actually going to be a JSON value. So here I want to be able to parse it by saying JSON.parse and convert this to an actual JSON or a JavaScript object instead of just a JSON. And when I store the value,

I just want to stringify it. So I can say json.stringify the value like this. So here I'm taking the value, whatever it is, an array, an object, a string, and I'm converting it to a JSON string. And then here, when I'm reading the value, I'm making sure I take it and convert it from a string into an actual JavaScript object, array, string, whatever it is. Now if I save, the first thing we'll need to do is we'll need to clear out our local storage because it's currently not using the correct formatting. So now if I refresh, everything should work. And you can see

That if I change some things here, change some things here, add a few hobbies into the list and refresh, everything is persisting properly. And that's again because we are doing everything with JSON. So we're making sure we convert everything to a string when we store it in local storage, because local storage only takes strings, and we're making sure we convert it back into JavaScript notation when we read it from our local storage. The only other thing that I like to do, and this is optional, it wasn't even in the README at all, is that if I have a value that's undefined, so if for example our value

Is equal to undefined. Well, in that case, I generally like to remove it from local storage. So I'm going to say remove the item at that key. Otherwise, do the code inside of here. So only when my value is undefined, that means I probably don't want to keep it in my local storage anymore. It doesn't really make sense to store undefined in your local storage. So in that case, I like to remove it. But it's entirely up to you if you want to do that or not. Also, another important thing to note is that our use effect should also change if our key changes. If for some reason we pass in a different key.

Right here, we obviously want to rerun this use effect because we need to make sure that our use effect is always storing to the correct key location.

—-------------------------------  
Sneha Mehra (00:00:00)  
One of the hardest parts about React is changing your mind to a more component-based system of thinking as opposed to the more traditional JavaScript-based way of thinking. And that's why this entire section of the course is going to be about how you can use components and most importantly how to start thinking in components, which is why this very first video is entirely about how to think about your application as components instead of as an entire application. So I have quite a few examples I want to go through, and this very first one comes straight from the React documentation.

And it's breaking apart this simple application into a few different components. So we just have essentially a search table for different products that we can search for. So as you can see, this gray outline surrounds our entire page, our entire application. And we've just called it a filterable product table. It doesn't really matter what the names on the right hand side are. You just know that this gray section right here is kind of surrounding the entire app. So components can encapsulate small things or large things. In our case, this component encapsulates the entirety of this table with the search bar and everything.

Then what we do is we break that down even further into smaller and smaller components. And that's the entire idea about your application. Your entire application is one component, everything inside of it is a component. Literally every single thing in React is a component of various different sizes. So we have our large overall application, then our blue outline here around our search section. This is just for our search bar. So it has our search, it has our only showing products that are in stock. And if you had more filters, you could put those inside this section.

And if it started to get even more complex, you could break it down even further, like make your search bar its own component or make this checkbox section its own component. It really depends on how you actually build your application. There's no like right or wrong way on where you should break apart your different components. It's really about how you feel it should be broken apart and how it makes most sense for your application. So in this case, we have our entire application, we have this search bar. Next we have this purple section, which is the entire product table. And again, we're just trying to kind of separate things that live on their own.

Anything that's its own section or its own piece, you make its own component. That's why the table is separate from the search bar because they're two separate things. Then we take this a step further where we have our category row and our actual row itself for each product. And you can see that these are separated again. They're very similar to each other, but there are some differences. For example, this green one, the category, only has one element, it's centered, it's bold. While these other rows, they have two elements inside of them, they're not centered, they're not bold. So there's quite a few differences, which is why they are separate components.

Sneha Mehra (00:02:19)  
You could make them the same component, but it really depends on your own needs. And I think in this case it makes the most sense to separate them into two different components. So this right here is a really basic example. So I want to show you a little bit more of a complex example of how you would do it in more of a larger scale real world application. So this is just the dashboard for my YouTube channel. As you can see, this is just showing some of my older videos. And how I would break this down as different components is as follows. So the first component I would have is this header section. As you can see, it's entirely distinct from the application.

And it makes a lot of sense that this would be its own component. Now you will notice that this does not repeat anywhere in our application. Our header is only in one single spot, but it's okay. A component doesn't have to be something that only repeats, it just has to be its own piece of logic or its own piece of UI. And in our case, this header section is definitely its own piece of UI. We could even take this a step further and make different pieces of this their own component as well. For example, when I click this create button, you can see I get this drop down right here.

It makes a lot of sense that this drop-down system would be something that's its own component as well. So now we have this drop-down component inside of our header component. The next thing that we have is this search bar. That could also be its own component if we wanted. Same thing with this sidebar. This kind of makes sense that it's its own component because everything on the side here is on its own and is connected to this button. So these two things are inside different components. For example, this sidebar section here that expands and collapses, and this collapse button, which is in the header.

They're in two different spots, but they can still talk to each other, which is one really nice thing about React, is you can have your components communicate between each other. So it doesn't have to be just things that are related to each other in one component. You can have multiple things in separate components and still have them talk to one another. Now if we move on a little ways, we have some things that are more repeated. For example, in the side the sidebar, we have a lot of sections that have an icon on the left and then some text on the right-hand side. This is a perfect example of a component. Each one of these rows would be its own component because they repeat that logic and that UI multiple times.

So it makes sense to put it into its own component. Now, if we move on to this row section where we have all the videos listed, this again is something that makes a lot of sense to have its own component. Each row is essentially its own individual component. And inside that, we may even have components inside these rows. For example, when I hover over this section, you notice I get all these icons showing up. Again, that makes sense that that would be some kind of like hover-based component that's going to be only for this section. So again, we have nested components inside of each other.

Sneha Mehra (00:04:34)  
This tab system at the top would also be its own component, same with this filtering list. That's again another component-based system. So pretty much every single thing in your app you can break down into individual components. The important thing is you don't break it down too far though. For example, here we just have this text that says none. It doesn't really make sense to have that be its own component. It's just an individual piece of text that's really too small to be a component, and it just clutters your application if you have too many components for things that don't need to be components. So now what I want you to do is I actually want you to try this thinking yourself.

I'm to show you this React webpage. This is like the current documentation for React. They're currently working on these beta docs, which should be out pretty soon. But I think this is a really good example of something you can work on to see if you can figure out how it is broken down into components. So I'm going to scroll through the page and I'm going to have you think of the different components in your head that you see on this page, and I'll show you what I see myself. So I'm just going to scroll through really quickly here. We have this top section. Then we have this section with some different examples. And then we have the footer at the very bottom.

So I'm gonna just scroll through that again one more time, just on the way back up, a little bit slower this time. So you can really kind of take in all the different things inside this application. And while I'm doing this, again, think about the different components you see, and then I'm gonna show you what I think. So if you're still thinking about it, pause the video. Otherwise, I'm gonna show you exactly what I think I see. So the very first thing that I see here is this header section. Most applications, I'm gonna be honest, that you work in are probably gonna have some type of header. So again, it makes sense that this header navigation section is its own component.

Next, we have this big section in the middle where it has like the React and the Get Started button. This is like a hero section here. I would also separate this out into its own component because it's kind of like its own chunk of information and it's separate from everything else. Again, these things aren't repeated anywhere else, but it makes sense that they would be their own component. Now the next thing we have is a repeated section. You can see we have these three ⁓ vertical column-based blocks of text. I would consider each one of these their own component because they share the exact same UI. You can see you have this kind of like lighter header.

as well as some text. Same thing here, same thing here. It's the exact same UI. So it makes sense to break these into three separate components because if I want to change this text to be bold, I want it to change on all three of them. So again that makes sense it would be its own individual component. That's another thing to think about when you have things that are repeated, think if I change one of these, do I want all of them to change? If you want all of them to change, then they should all be the same component. But if you don't want all of them to change then maybe they aren't actually as similar as you think they are and they should probably be different components.

Sneha Mehra (00:06:55)  
or maybe not even a component at all. And the next section I see is kind of each one of these rows where you have this text on the left hand side, you have this editor in the middle, and you have this results section on the right. Again, I see this whole entire thing as one component. And then you can break it down even further where you have a text section on the left and then you have this editor on the right. And then the editor can even be broken down further where you have the actual text editor and then the results section on the right. So again this is one big component which contains smaller components inside of it.

So as you see, we have multiple of those different kinds of components. And then finally at the bottom we have this footer section, which again I would make its own component based on the footer. And inside here you have even more components because each one of these vertical sections of text would be its own component. Because if I change, for example, the style of this header section to be a white color, I would want it to update on all of the different vertical sections, not just one of them. Now I know this is really overwhelming to try to start thinking in components like this. It's not something that comes supernaturally.

But as you start working in React more throughout this course, it's gonna become much more natural how you think about components. And hopefully by the end of this course, you'll be seeing components left and right.

—------------------------------

Sneha Mehra (00:00:00)  
Up until this point, we've had absolutely no way to render out elements to our screen that are inside of an array. So let's say we have an array of items. Here we have an ID and a name, and we wanted to render out each one of those items to our screen. We wanted to loop through them, render them out as some type of JSX, you know, component, whatever it is. Right now we have absolutely no way to do that because if I try to just render out these items, as you can see here, if I just change this to items, you're gonna see immediately I get an error. If I inspect my page, bring this over and go over to my console, you can see we get an error because it's trying to render out an object.

So by default, when you pass an array into your JSX, what React tries to do is it tries to render out each element inside of that array. So if I pass it an array here of just like numbers, this is something React can actually render. You can see it renders out each one of those elements inside the array right next to each other. So that's working perfectly fine. Now if we take this array and we actually change it to be JSX, so we have a bunch of divs here. So let me just get my code copied down for all these different divs.

There we go. And I save this. You can now see these are actually rendered out as divs. If I inspect, you can see we have four different divs that are rendering out to our screen. So that's working perfectly fine. So what we need to do is we need to convert this array of objects into something that React can understand. So we need to convert it to React.jsx elements. And the best way to do that is by using a map. So what we can do here, instead of rendering out some JSON, is we can actually render out our items, but we want to map over those items. So we're going to have each one of our items.

And what we want to do is we want to return from here some type of JSX. So I could have a div that just says item.name. And now if I save, you can show you it says item one and item two. And if I click add item, you can see it's adding a brand new item every single time, which is what this code right here is doing. It just creates a new item with the name of new item. Now this is super useful because by using map, what we're doing is returning JSX. So we're converting our array to an array of JSX, which React actually knows how to handle. And it's super common to have your code just automatically return like this.

So, what a lot of people will do is they'll actually remove these curly braces and replace them with parentheses instead, just like this. And then they'll also remove this return statement. And what that does is just automatically returns whatever you put inside the parentheses. When I save, you can now see we get the exact same result on our screen. It's working exactly the same. I can even make this on one line if I wanted by doing this. And you can see that now it's on one line. It's automatically formatting onto multiple lines with Prettier just because it's too long to fit on one line, but I could put it on one line if I want.

Sneha Mehra (00:02:18)  
These parentheses just allow me to wrap it onto multiple lines yet still return this element here. It's up to you if you want to do this or not. You're just going to see this a lot, which is why I'm mentioning it. Personally, when you're getting started, I would recommend using the normal curly brace technique, as this is just more common in what you're going to be used to with normal JavaScript, and it works exactly the same. And the amount of code you have to write that's extra is literally one extra word of return, so it's not a huge deal. And the really nice thing is if you actually need to do things up here like console.log each item.

Now you can do that while in the other way you'd have to change to this curly brace syntax to do something like that. Now this may seem like that's all there is to rendering out list in React, but it's a little bit more complex than this actually. If we come in here and we change our add item to change it so that it's actually adding the item at the beginning of the list instead of the end of the list, we're gonna see some interesting problems. So I'm just gonna copy this code like this, move it over here, and we're just gonna give this quick save, a refresh, and we'll click add item. And you can see it's adding our items just fine to the beginning of the list.

What happens if we have some inputs, for example, inside of our code that we're dealing with? So here I'm just gonna add in an input.

Input type equals text, just like that. So now each one of these have an input after them. Let's just refresh our page and I'm gonna add some ⁓ text into that input and we'll click add item. You're gonna notice that the input is actually stayed the same for this top one instead of being you know pushed down to the bottom, like it should. You'd think that this would be the input, but you notice the inputs are just staying at the top instead of actually being pushed down because they're tied to this component. The reason for that is by default, React.

tries to figure out which one of your elements is associated with which item in your array. And to do that, it associates each index of the array with a specific set of components. So what happens is the index zero, which in our case is the first element, has this input element and it has this, you know, text or whatever associated with it. This entire return right here is associated with the index of zero. So when I add a new element to the beginning of my list by clicking this right here.

Sneha Mehra (00:04:13)  
You can see that what happens is that input that's still associated with index zero stays with index zero, even though you know the name is changing because the actual name element of our item is changing. So that's a big problem. So what you can do inside of React is you actually need to pass a key to every single element in an array. If we inspect our page, you're actually gonna see we have an error. We go to our console, each child in the list much should have a unique key prop. If you don't pass a key, by default, it uses the index, but you should never use the index unless it's the only option you have.

And even then, it's definitely last resort, which has bugs like this, as you can see. So what you need to do is you need to cast a key prop to each element. It's going to be the very first element you return, which in our case is our div. ⁓ And inside of here, you just need to make sure it is unique. In our case, all of our different elements have a unique ID, so we can say item.id. So now if I save, I'm going to refresh, I'm going to put some text inside of here, and I click add item. You can see that this input is correctly linked up with this item right here. So this key is just telling React.

what the actual identifier is. So no matter what changes in your array, whether the order changes, whether elements are moved around, doesn't matter what happens. It just says, okay, this ID is associated with this specific, you know, return right here. So it'll know that next time you render out that element, whatever the ID is, it'll match all of the different stuff that you need. So all the things like state and input values will stay the same and move around like you want them to.

Now this may seem really confusing, but really all React is doing behind the scenes is just hooking up your elements in your array with the unique keys that you give the elements that you want to return. So you just need to make sure that all the keys that you use are unique. You can't have the same key. For example, if I just said my key was one, obviously this is a problem. If I inspect, I'm going to get an error that's saying all the keys must be unique. But they only need to be unique for each instance of where you render this out. For example, if I rendered this text here twice, just like this, and I clicked save.

You can see if I inspect my page, I'm not going to get any errors at all. If I just refresh this, you can see I have no errors at all. No matter what I do adding items, everything is working perfectly fine with adding everything around. There's no errors. And the reason for that is because your keys only need to be unique for the actual instance that you're looping through. So in our case, each one of these loops right here is a separate instance. So even though they have the same keys between them, it's okay because they are separate. But each one of the instances needs to be unique. So in our case, all these IDs must be unique, which in our case they are because they're randomly generated.

Sneha Mehra (00:06:31)  
Another thing that's important is obviously the ID needs to stay the same between each one of the renders. And that's because if they changed every single time you rendered, React would have no way to hook things up, so obviously nothing would work. And it doesn't matter what you're doing here, whether you're returning a div, whether you're returning a custom component like item, you just need to make sure you always pass it, you know, the key like this. So if you had a custom component called item, you'd pass it the key just like this. You do not set the key inside the component. For example, if I had my item component, export.

Function item, I would not come in here and actually set the div or set the key. Sorry, so if I had a div,

Sneha Mehra (00:07:12)  
Close that off, there we go. This is not the correct way to do this. The key must always be defined where you're actually using the loop. So in our case, here's our loop right here. So that's where the key must be defined. It cannot be defined inside the actual child components. So this would just need to be moved to up here, and now it would work just fine.

—------------------------  
Sneha Mehra (00:00:00)  
Now in the last couple videos we really dove into how you can deal with complex state and how you can share state easily between multiple different components even globally across your entire application. This actually leads to a lot of problems where people share too much state globally or they don't put their state locally enough. So I've gone back to a really simple example. I just have an app that renders out a counter and inside of our counter we're storing the state for our account. This is super straightforward and easy. And if for example we wanted to add a reset button, it's really easy because we only need to change this one file.

For example, I just add a new button. I change this to say set count to zero. And I change this to say reset. And now I can click reset and it resets me to zero. So the really nice thing is since all of my state is stored inside this component, if I want to make changes to how that state works, it all happens inside of one single file. Super straightforward, super easy. Now the problem I see though is when people start to realize, you know, I can store state in context or I can store it, you know, much more globally, they do something like this instead. They take their count state and they put it inside their app.

And they take all the things for dealing with changing their state for their count and they put it inside of here. So they'll have a function that's like change count. We'll give it an amount. And it'll just call set count or takes the count and it'll just take the count and add that amount to it. Super straightforward. And then they'll pass in the count here. And they'll pass in the change count like this. Change count, there we go. And then inside the counter, they'll take that in. ⁓ Count, change count.

And this is going to call change count with a minus one. This would call change count with a plus one. And for now, we're just going to remove that reset. So if I do this, I make sure that I import use state inside of here and I say everything is working exactly the same as it did before. So it doesn't matter where you store your state, as long as you hook everything up properly, it's going to work just the same as before. But what I've done here is essentially I've taken my state and I've taken it out of the component where it's actually used, and I've stored it inside of a parent component.

And this is great if you need to share that state between lots of other components or do other information with it. But in our case, this state right here is used in one single place. It's used in this counter component. So why are we storing it inside of this parent component? Generally, inside of React, when you're storing state, it's best to store that state as locally as possible. And by that I mean essentially store all of your state inside the component that it's being used or as close to the components that it's being used in as possible. In our case,

Sneha Mehra (00:02:27)  
We can move all this state into our counter because that's where it's actually being used. And then it'll be as local as possible. And again, that makes it easy because you only need to change one single file when you need to update and make that state work differently. In our case, if we wanted to add a reset function, well I need to add that to my app here. So I'd set my count to zero. And then what I would need to do is inside of here, I would need to add that reset function as a prop, just like that. And then here I would need to take that in.

We go, and I would need to add a button called reset. That calls the reset function. And now you can see that reset function is working, but I had to change both my counter and my app in the GIN. That's because I stored my state essentially more globally than I needed to. I could have just stored it in my counter as local as possible, but I decided to put it in my app instead. Now obviously if you're looking at this, you're probably thinking, well, of course I'm gonna put the counter state inside the counter. And in this example, it's really easy to see that and see why that is.

But as you start to get to larger and larger applications with more and more state inside of it, it can become difficult because you may accidentally put your state in the wrong spot and you may not even realize that it could be more local than it is. That's something you always need to think about and consider is can I make this more local than it is? If so, probably about 90% of the time you should because it'll make your code easier to work with, since all that state will be localized and you won't have to worry about it in the more global state context.

This also works with the idea of context. People like to put essentially everything inside of a context, and that's not a good idea. Obviously, storing this counter information in a context doesn't make any sense at all because I only use it in one single component. So there's no point in putting it inside of a context. But even if I used it in a couple different components, it still may not make sense to put it in a context just because it doesn't need to be shared super deeply or super widely across my application. Context should really be used in situations where it is prohibitive to actually pass it down as props.

Because doesn't make sense to go through a bunch of different layers of prop drilling, or if it's something that needs to be used all over your application and like different areas of your application, single sections here and there, then it's a really good idea to use context. But otherwise, it's not a good idea just to put everything in context because then everything is global and it's really hard to deal with. Now there are situations though where local state can't actually be stored in the component that it's used. For example, if we actually changed our counter so that we had some other buttons in a different location, like for example, if we came into here, we just

Sneha Mehra (00:04:52)  
Give this so we can return multiple things. And instead I had a component called counter reset. And this counter reset is what's going to reset my actual counter. So let's create a really simple counter reset component.

Sneha Mehra (00:05:10)  
There we go. And inside of here, all I want to do is call a reset function. So we'll pass along that reset function. And it's essentially just going to be this button right here. I'm just going to move that into here. There we go. Now we can save our counter. It's no longer getting our reset passed in. And let's just make sure we import this counter reset. There we go. And it looks like in here my return is in the wrong location. There we go. So now what I've done, if we look at our app and we check it, you can see it looks like it's not working because I need to make sure I take a reset and I pass it to the right component. So

There we go. So now if I click reset, you can see it's resetting back to zero. So what I've essentially done is I've taken it so instead of our counter being just in one component, it's now in two components. Obviously, in this example, it doesn't make sense to break this apart, but imagine you're in a larger application where obviously it makes sense to break into two different components your logic. So now we have part of our counter here, and part of our counter state is being used here. So obviously, we need to store our counter state in a location where both of these components can access it. And in that case,

Storing it in this app component makes sense because this is the component that is the closest to where it is used. So this counter state is used in both these components. So obviously, this parent component is the one that is as close as possible to being as local as it can be. It can't obviously be stored in the counter because then we would have no way to reset it from this component. So we need to move it up to the parent. And essentially, when you're trying to make state as local as possible, what I like to do is I constantly try to put it inside of the next child, inside of the next child, inside of the next child, till I get to the point where

Putting it inside the next child, like putting it in this counter, no longer works because there's other components it's trying to access that need that data that no longer have access to that data. So this is a scenario where your state is as local as possible, but it's not actually stored inside the components that are using it. This is something you're gonna run into all the time, and something we've already run into all the time with the different like to-do list applications that we've built. We've had to store our to-do list inside of the app component because the other components, like the to-do list items,

Needed that information as well as the app component itself needed the information. Now overall, I don't want this to be something you spend too much time worrying about or thinking about because it's just a way to help make your code a little bit cleaner. But as you're writing your code, just have in the back of your mind the thought of, okay, is this stored as locally as possible? And if it's not, is there a reason that I'm not storing it there? Is it because it needs to be accessed by multiple components, or is it because it just makes more sense to store it somewhere else? But generally, if your storing state, the more local it is, the easier it is to change.

Sneha Mehra (00:07:33)  
and modify your code in the future, which is really what you want to have clean and well-written code.

—--------------------------------  
Sneha Mehra (00:00:00)  
Up until this point, we've had absolutely no way to render out elements to our screen that are inside of an array. So let's say we have an array of items. Here we have an ID and a name, and we wanted to render out each one of those items to our screen. We wanted to loop through them, render them out as some type of JSX, you know, component, whatever it is. Right now we have absolutely no way to do that because if I try to just render out these items, as you can see here, if I just change this to items, you're gonna see immediately I get an error. If I inspect my page, bring this over and go over to my console, you can see we get an error because it's trying to render out an object.

So by default, when you pass an array into your JSX, what React tries to do is it tries to render out each element inside of that array. So if I pass it an array here of just like numbers, this is something React can actually render. You can see it renders out each one of those elements inside the array right next to each other. So that's working perfectly fine. Now if we take this array and we actually change it to be JSX, so we have a bunch of divs here. So let me just get my code copied down for all these different divs.

There we go. And I save this. You can now see these are actually rendered out as divs. If I inspect, you can see we have four different divs that are rendering out to our screen. So that's working perfectly fine. So what we need to do is we need to convert this array of objects into something that React can understand. So we need to convert it to React.jsx elements. And the best way to do that is by using a map. So what we can do here, instead of rendering out some JSON, is we can actually render out our items, but we want to map over those items. So we're going to have each one of our items.

And what we want to do is we want to return from here some type of JSX. So I could have a div that just says item.name. And now if I save, you can show you it says item one and item two. And if I click add item, you can see it's adding a brand new item every single time, which is what this code right here is doing. It just creates a new item with the name of new item. Now this is super useful because by using map, what we're doing is returning JSX. So we're converting our array to an array of JSX, which React actually knows how to handle. And it's super common to have your code just automatically return like this.

So, what a lot of people will do is they'll actually remove these curly braces and replace them with parentheses instead, just like this. And then they'll also remove this return statement. And what that does is just automatically returns whatever you put inside the parentheses. When I save, you can now see we get the exact same result on our screen. It's working exactly the same. I can even make this on one line if I wanted by doing this. And you can see that now it's on one line. It's automatically formatting onto multiple lines with Prettier just because it's too long to fit on one line, but I could put it on one line if I want.

Sneha Mehra (00:02:18)  
These parentheses just allow me to wrap it onto multiple lines yet still return this element here. It's up to you if you want to do this or not. You're just going to see this a lot, which is why I'm mentioning it. Personally, when you're getting started, I would recommend using the normal curly brace technique, as this is just more common in what you're going to be used to with normal JavaScript, and it works exactly the same. And the amount of code you have to write that's extra is literally one extra word of return, so it's not a huge deal. And the really nice thing is if you actually need to do things up here like console.log each item.

Now you can do that while in the other way you'd have to change to this curly brace syntax to do something like that. Now this may seem like that's all there is to rendering out list in React, but it's a little bit more complex than this actually. If we come in here and we change our add item to change it so that it's actually adding the item at the beginning of the list instead of the end of the list, we're gonna see some interesting problems. So I'm just gonna copy this code like this, move it over here, and we're just gonna give this quick save, a refresh, and we'll click add item. And you can see it's adding our items just fine to the beginning of the list.

What happens if we have some inputs, for example, inside of our code that we're dealing with? So here I'm just gonna add in an input.

Input type equals text, just like that. So now each one of these have an input after them. Let's just refresh our page and I'm gonna add some ⁓ text into that input and we'll click add item. You're gonna notice that the input is actually stayed the same for this top one instead of being you know pushed down to the bottom, like it should. You'd think that this would be the input, but you notice the inputs are just staying at the top instead of actually being pushed down because they're tied to this component. The reason for that is by default, React.

tries to figure out which one of your elements is associated with which item in your array. And to do that, it associates each index of the array with a specific set of components. So what happens is the index zero, which in our case is the first element, has this input element and it has this, you know, text or whatever associated with it. This entire return right here is associated with the index of zero. So when I add a new element to the beginning of my list by clicking this right here.

Sneha Mehra (00:04:13)  
You can see that what happens is that input that's still associated with index zero stays with index zero, even though you know the name is changing because the actual name element of our item is changing. So that's a big problem. So what you can do inside of React is you actually need to pass a key to every single element in an array. If we inspect our page, you're actually gonna see we have an error. We go to our console, each child in the list much should have a unique key prop. If you don't pass a key, by default, it uses the index, but you should never use the index unless it's the only option you have.

And even then, it's definitely last resort, which has bugs like this, as you can see. So what you need to do is you need to cast a key prop to each element. It's going to be the very first element you return, which in our case is our div. ⁓ And inside of here, you just need to make sure it is unique. In our case, all of our different elements have a unique ID, so we can say item.id. So now if I save, I'm going to refresh, I'm going to put some text inside of here, and I click add item. You can see that this input is correctly linked up with this item right here. So this key is just telling React.

what the actual identifier is. So no matter what changes in your array, whether the order changes, whether elements are moved around, doesn't matter what happens. It just says, okay, this ID is associated with this specific, you know, return right here. So it'll know that next time you render out that element, whatever the ID is, it'll match all of the different stuff that you need. So all the things like state and input values will stay the same and move around like you want them to.

Now this may seem really confusing, but really all React is doing behind the scenes is just hooking up your elements in your array with the unique keys that you give the elements that you want to return. So you just need to make sure that all the keys that you use are unique. You can't have the same key. For example, if I just said my key was one, obviously this is a problem. If I inspect, I'm going to get an error that's saying all the keys must be unique. But they only need to be unique for each instance of where you render this out. For example, if I rendered this text here twice, just like this, and I clicked save.

You can see if I inspect my page, I'm not going to get any errors at all. If I just refresh this, you can see I have no errors at all. No matter what I do adding items, everything is working perfectly fine with adding everything around. There's no errors. And the reason for that is because your keys only need to be unique for the actual instance that you're looping through. So in our case, each one of these loops right here is a separate instance. So even though they have the same keys between them, it's okay because they are separate. But each one of the instances needs to be unique. So in our case, all these IDs must be unique, which in our case they are because they're randomly generated.

Sneha Mehra (00:06:31)  
Another thing that's important is obviously the ID needs to stay the same between each one of the renders. And that's because if they changed every single time you rendered, React would have no way to hook things up, so obviously nothing would work. And it doesn't matter what you're doing here, whether you're returning a div, whether you're returning a custom component like item, you just need to make sure you always pass it, you know, the key like this. So if you had a custom component called item, you'd pass it the key just like this. You do not set the key inside the component. For example, if I had my item component, export.

Function item, I would not come in here and actually set the div or set the key. Sorry, so if I had a div,

Sneha Mehra (00:07:12)  
Close that off, there we go. This is not the correct way to do this. The key must always be defined where you're actually using the loop. So in our case, here's our loop right here. So that's where the key must be defined. It cannot be defined inside the actual child components. So this would just need to be moved to up here, and now it would work just fine.

—-----------------------  
Sneha Mehra (00:00:00)  
Now in the last couple videos we really dove into how you can deal with complex state and how you can share state easily between multiple different components even globally across your entire application. This actually leads to a lot of problems where people share too much state globally or they don't put their state locally enough. So I've gone back to a really simple example. I just have an app that renders out a counter and inside of our counter we're storing the state for our account. This is super straightforward and easy. And if for example we wanted to add a reset button, it's really easy because we only need to change this one file.

For example, I just add a new button. I change this to say set count to zero. And I change this to say reset. And now I can click reset and it resets me to zero. So the really nice thing is since all of my state is stored inside this component, if I want to make changes to how that state works, it all happens inside of one single file. Super straightforward, super easy. Now the problem I see though is when people start to realize, you know, I can store state in context or I can store it, you know, much more globally, they do something like this instead. They take their count state and they put it inside their app.

And they take all the things for dealing with changing their state for their count and they put it inside of here. So they'll have a function that's like change count. We'll give it an amount. And it'll just call set count or takes the count and it'll just take the count and add that amount to it. Super straightforward. And then they'll pass in the count here. And they'll pass in the change count like this. Change count, there we go. And then inside the counter, they'll take that in. ⁓ Count, change count.

And this is going to call change count with a minus one. This would call change count with a plus one. And for now, we're just going to remove that reset. So if I do this, I make sure that I import use state inside of here and I say everything is working exactly the same as it did before. So it doesn't matter where you store your state, as long as you hook everything up properly, it's going to work just the same as before. But what I've done here is essentially I've taken my state and I've taken it out of the component where it's actually used, and I've stored it inside of a parent component.

And this is great if you need to share that state between lots of other components or do other information with it. But in our case, this state right here is used in one single place. It's used in this counter component. So why are we storing it inside of this parent component? Generally, inside of React, when you're storing state, it's best to store that state as locally as possible. And by that I mean essentially store all of your state inside the component that it's being used or as close to the components that it's being used in as possible. In our case,

Sneha Mehra (00:02:27)  
We can move all this state into our counter because that's where it's actually being used. And then it'll be as local as possible. And again, that makes it easy because you only need to change one single file when you need to update and make that state work differently. In our case, if we wanted to add a reset function, well I need to add that to my app here. So I'd set my count to zero. And then what I would need to do is inside of here, I would need to add that reset function as a prop, just like that. And then here I would need to take that in.

We go, and I would need to add a button called reset. That calls the reset function. And now you can see that reset function is working, but I had to change both my counter and my app in the GIN. That's because I stored my state essentially more globally than I needed to. I could have just stored it in my counter as local as possible, but I decided to put it in my app instead. Now obviously if you're looking at this, you're probably thinking, well, of course I'm gonna put the counter state inside the counter. And in this example, it's really easy to see that and see why that is.

But as you start to get to larger and larger applications with more and more state inside of it, it can become difficult because you may accidentally put your state in the wrong spot and you may not even realize that it could be more local than it is. That's something you always need to think about and consider is can I make this more local than it is? If so, probably about 90% of the time you should because it'll make your code easier to work with, since all that state will be localized and you won't have to worry about it in the more global state context.

This also works with the idea of context. People like to put essentially everything inside of a context, and that's not a good idea. Obviously, storing this counter information in a context doesn't make any sense at all because I only use it in one single component. So there's no point in putting it inside of a context. But even if I used it in a couple different components, it still may not make sense to put it in a context just because it doesn't need to be shared super deeply or super widely across my application. Context should really be used in situations where it is prohibitive to actually pass it down as props.

Because doesn't make sense to go through a bunch of different layers of prop drilling, or if it's something that needs to be used all over your application and like different areas of your application, single sections here and there, then it's a really good idea to use context. But otherwise, it's not a good idea just to put everything in context because then everything is global and it's really hard to deal with. Now there are situations though where local state can't actually be stored in the component that it's used. For example, if we actually changed our counter so that we had some other buttons in a different location, like for example, if we came into here, we just

Sneha Mehra (00:04:52)  
Give this so we can return multiple things. And instead I had a component called counter reset. And this counter reset is what's going to reset my actual counter. So let's create a really simple counter reset component.

Sneha Mehra (00:05:10)  
There we go. And inside of here, all I want to do is call a reset function. So we'll pass along that reset function. And it's essentially just going to be this button right here. I'm just going to move that into here. There we go. Now we can save our counter. It's no longer getting our reset passed in. And let's just make sure we import this counter reset. There we go. And it looks like in here my return is in the wrong location. There we go. So now what I've done, if we look at our app and we check it, you can see it looks like it's not working because I need to make sure I take a reset and I pass it to the right component. So

There we go. So now if I click reset, you can see it's resetting back to zero. So what I've essentially done is I've taken it so instead of our counter being just in one component, it's now in two components. Obviously, in this example, it doesn't make sense to break this apart, but imagine you're in a larger application where obviously it makes sense to break into two different components your logic. So now we have part of our counter here, and part of our counter state is being used here. So obviously, we need to store our counter state in a location where both of these components can access it. And in that case,

Storing it in this app component makes sense because this is the component that is the closest to where it is used. So this counter state is used in both these components. So obviously, this parent component is the one that is as close as possible to being as local as it can be. It can't obviously be stored in the counter because then we would have no way to reset it from this component. So we need to move it up to the parent. And essentially, when you're trying to make state as local as possible, what I like to do is I constantly try to put it inside of the next child, inside of the next child, inside of the next child, till I get to the point where

Putting it inside the next child, like putting it in this counter, no longer works because there's other components it's trying to access that need that data that no longer have access to that data. So this is a scenario where your state is as local as possible, but it's not actually stored inside the components that are using it. This is something you're gonna run into all the time, and something we've already run into all the time with the different like to-do list applications that we've built. We've had to store our to-do list inside of the app component because the other components, like the to-do list items,

Needed that information as well as the app component itself needed the information. Now overall, I don't want this to be something you spend too much time worrying about or thinking about because it's just a way to help make your code a little bit cleaner. But as you're writing your code, just have in the back of your mind the thought of, okay, is this stored as locally as possible? And if it's not, is there a reason that I'm not storing it there? Is it because it needs to be accessed by multiple components, or is it because it just makes more sense to store it somewhere else? But generally, if your storing state, the more local it is, the easier it is to change.

Sneha Mehra (00:07:33)  
and modify your code in the future, which is really what you want to have clean and well-written code.

—----------------------  
Sneha Mehra (00:00:00)  
All I've done here is change our code to a class component. So now you can see our state is stored in a class component state and we're rendering out just like we would in a normal class component. And that's because I want to show you how to use refs inside of class components. It's actually incredibly easy. The first thing I actually want to mention though is how you actually store values that don't cause re-renders because it's different in class components. That's because you don't even need a ref to do this. I can just say this.value. ⁓

equals seven for example and now I have access to that and changing that value for example in here if I said that my value is now equal to eight that's not going to cause any types of re-renders anything like that. So if you want to store a value that is specific to your component but doesn't cause re-renders or anything like that just store it on the this property and that's going to be how you do that inside of a class component. But that's not what you're here for on this video. You're here to learn how to use refs inside of class components and it's really quite straightforward. If we wanted to get an input ref, all you would need to do is say React whoops

React.create ref and that's going to create you a brand new reference called input ref. And then we can just set it just like we did inside of a function component by saying ref is equal to that. And now we have our input ref. And what I could do is I could just come in here and I could say component will mount. Whoops, component ⁓ did mount. Sorry, there go. ⁓ And in here I could say input ref dot current ⁓ dot focus. You use the same current and everything. Everything works exactly the same. This is just how you get it instead of using a hook.

That's really the only difference. And now if I refresh my page and I make sure that I actually set this on the this property. Sorry. So this should be this.input ref. That way I can access it down here by saying this.input ref and this.input ref. Now if I save and I refresh my page, you can see it automatically focuses me inside of that input. So really the only thing you need to do is you just need to make sure you set a variable on your overall class, this object, by calling react.create ref, and then everything after that works exactly the same as it does inside of a function component.

The only difference is you don't need to use a reference for storing things like strings or objects and so on, because you can just do that on the object by just saying this dot value equals whatever you want it to equal.

—-------------------------------------

Sneha Mehra (00:00:00)  
Up until now, when we've been dealing with forms, we've pretty much been exclusively using state to deal with our inputs, which is fine and is how most people deal with forms inside of React, but it's not always the best way to deal with forms. For this example, I have really simplified code here. We just have a simple state for our name. We have a handle submit that'll just alert out our name as long as we type in a name. And then we have our form which has a label, it has our input, which has a value and an on change, which is updating our state for us, and then we have a button just to submit our form.

So if I just type in Kyle, for example, and I click alert name, you can see it prints out name of Kyle. And if I have no name and I click alert, it'll do nothing at all because of this simple if check here. This is really straightforward and a super common use case you'll see with forms. You can imagine that you have a bunch of state information, and then whenever you submit your form, you want to do something with that data, like call an API or something like that. In those cases, if this is all that your form is doing, it doesn't actually make sense to use state in those scenarios. For example, let's come in here and let's type out a use effect.

And want this use effect to run every single time my component renders. And I just want it to log out render, just like that. So now if we just inspect our page, we look at that console tab real quick. Make sure that I just clear everything out. And I give my page a refresh. You can see that it's printing out render twice just because of that strict mode. And now every time I change my name, you can see it's incrementing the count of my render, which is not a huge deal because it's not actually changing that much.

But it is causing my component to re-render every single time I type out a character in any of my inputs. But I don't actually care what the value of my name is until I submit my form. So there's no point in me constantly updating my state and re-rendering my component if I only need to get the information whenever some type of event occurs, like a handle submit. In those cases, it makes much more sense for me to actually use an uncontrolled input and use a ref to get the value instead.

Let me show you what that would look like. I'm just going to comment out the code for our current input information. I'm going to come in here with a brand new input. It's going to be very similar. So we're going to give it the same exact ID. We're going to give it the same exact type of text. But here, instead of doing a value in an on change, I'm going to be using a reference instead. So our reference here is going to be set to our input ref, or let's call it name ref, just like that. There we go. And I'm going to close that off. Then I'm going to come all the way up here. And we're just going to create that. So we're going to say const

Sneha Mehra (00:02:20)  
Name ref equals use ref, just like that. Make sure it's imported perfectly. So that looks really good. And now what I can do is where I used to use my name, I can just say that the const name is equal to name ref.current.value. Because that's going to get the value of my input. And now you'll notice that everything is going to work exactly the same as before. If I type in a name, hit alert, you can see it prints out the name. And if my name is blank and I click alert, it does nothing. But the big difference here, if I actually go to my console and clear everything out, is what happens when I

actually change my input. You can see we've rendered twice. Now it's clear that. And as I type in my input, nothing at all is changing. You can see I'm never doing a re-render at all. And then when I alert, it's obviously alerting that out. But again, there are no re-renders happening at all. My code is never being called again. So I'm actually saving a little bit on performance by using a ref as opposed to using state. And you would be surprised how commonly you create forms that work just like this where you only need access to the value whenever something happens, whether it's you submit your form or some other event happens.

It's not as often that you actually need access to the input all the time on every single re-render. So in cases like this where you don't need the input right away, I would recommend using a ref just to save a little bit on performance. And also it makes your code a little bit easier to read and understand because as you can see here, I got rid of this entire on change, which is kind of a pain to write. And all I have is this single ref here. So overall, it just makes the complexity of your code slightly simpler, and overall it improves your performance just a little bit.

—------------------------

Sneha Mehra (00:00:00)  
So here we have the starting code for the exercises that you're going to do. You're going to notice we have a pretty lengthy README that explains exactly what's going on. The biggest thing you'll notice is this starting code is actually a full VET project, and it's very similar to the project we've been working on because as you can see, our child has a pretty much all the same code that we have here. And in our app component, I just have the ability to show and hire our child by clicking on this button that'll allow you to simulate mounting and unmounting.

Now, also a really important thing to note is in this main.js, I've removed strict mode, and hopefully you've removed it for all the videos so far up to this point. It'll just make me working with all these use effects a lot easier to understand exactly what's going on. Because if you leave it on, it'll sometimes double log things. And again, I'm gonna cover exactly how this strict mode works in just a couple videos, so then we can start using it in our projects. But if we look at the README here, you can see that in the top here, I'm just telling you how to get started with this project. Make sure you install all the dependencies.

And then you'll also notice that I just talked about this show hide button here and the removal of strict mode. Now, for the actual things that you need to do, there's some instructions here, these first four steps that you should definitely do. And then I have two bonuses which are a little bit trickier, but I think you can do them. And the only reason they're trickier is because they're covering things that we haven't actually covered directly in any of the video yet. But hopefully, with a little bit of thinking and application, you can figure out these. But if you can't, again, no worries. So the very first thing I want you to do is I just want you to log out the text render every time the component re-renders.

Then log out the text high when the component mounts, then log out this text right here whenever the name or age changes, and update the document title anytime the name changes as well. For our bonuses, I want you to log out by when the component unmounts. Again, a little tricky because we haven't covered it. And then this one is a little confusing, so I want explain what's going on. Essentially, I want you to use a set timeout to log out the text my name is and whatever the name is, only after there's been a one-second delay in typing. So for example, if I come in here and my text starts out as Kyle.

And I really quickly delete two characters like that, you can see that there is less than one second between each change. So what happens is it waits until there's one second of delay between changing the name and then it'll print out whatever the current name is. So in our case, if I have Kyle here and I really quickly delete two characters, it'll print out ky after one second. Now in the other hand, if I delete the E and then wait a while, and then delete the L and then wait a while, it'll print out both KYL and KY one second after each one of those changes.

Sneha Mehra (00:02:16)  
If it's confusing, just read through this again and hopefully it'll make a little bit of sense. Essentially I'm just making a delay that's saying after one second of no changes at all happening in my input, print out whatever the value of the input is. That's what that final one is doing. And again, it's something we haven't covered, but hopefully you'll be able to figure it out.

—----------------------  
Sneha Mehra (00:00:00)  
Up until this point, we've had absolutely no way to render out elements to our screen that are inside of an array. So let's say we have an array of items. Here we have an ID and a name, and we wanted to render out each one of those items to our screen. We wanted to loop through them, render them out as some type of JSX, you know, component, whatever it is. Right now we have absolutely no way to do that because if I try to just render out these items, as you can see here, if I just change this to items, you're gonna see immediately I get an error. If I inspect my page, bring this over and go over to my console, you can see we get an error because it's trying to render out an object.

So by default, when you pass an array into your JSX, what React tries to do is it tries to render out each element inside of that array. So if I pass it an array here of just like numbers, this is something React can actually render. You can see it renders out each one of those elements inside the array right next to each other. So that's working perfectly fine. Now if we take this array and we actually change it to be JSX, so we have a bunch of divs here. So let me just get my code copied down for all these different divs.

There we go. And I save this. You can now see these are actually rendered out as divs. If I inspect, you can see we have four different divs that are rendering out to our screen. So that's working perfectly fine. So what we need to do is we need to convert this array of objects into something that React can understand. So we need to convert it to React.jsx elements. And the best way to do that is by using a map. So what we can do here, instead of rendering out some JSON, is we can actually render out our items, but we want to map over those items. So we're going to have each one of our items.

And what we want to do is we want to return from here some type of JSX. So I could have a div that just says item.name. And now if I save, you can show you it says item one and item two. And if I click add item, you can see it's adding a brand new item every single time, which is what this code right here is doing. It just creates a new item with the name of new item. Now this is super useful because by using map, what we're doing is returning JSX. So we're converting our array to an array of JSX, which React actually knows how to handle. And it's super common to have your code just automatically return like this.

So, what a lot of people will do is they'll actually remove these curly braces and replace them with parentheses instead, just like this. And then they'll also remove this return statement. And what that does is just automatically returns whatever you put inside the parentheses. When I save, you can now see we get the exact same result on our screen. It's working exactly the same. I can even make this on one line if I wanted by doing this. And you can see that now it's on one line. It's automatically formatting onto multiple lines with Prettier just because it's too long to fit on one line, but I could put it on one line if I want.

Sneha Mehra (00:02:18)  
These parentheses just allow me to wrap it onto multiple lines yet still return this element here. It's up to you if you want to do this or not. You're just going to see this a lot, which is why I'm mentioning it. Personally, when you're getting started, I would recommend using the normal curly brace technique, as this is just more common in what you're going to be used to with normal JavaScript, and it works exactly the same. And the amount of code you have to write that's extra is literally one extra word of return, so it's not a huge deal. And the really nice thing is if you actually need to do things up here like console.log each item.

Now you can do that while in the other way you'd have to change to this curly brace syntax to do something like that. Now this may seem like that's all there is to rendering out list in React, but it's a little bit more complex than this actually. If we come in here and we change our add item to change it so that it's actually adding the item at the beginning of the list instead of the end of the list, we're gonna see some interesting problems. So I'm just gonna copy this code like this, move it over here, and we're just gonna give this quick save, a refresh, and we'll click add item. And you can see it's adding our items just fine to the beginning of the list.

What happens if we have some inputs, for example, inside of our code that we're dealing with? So here I'm just gonna add in an input.

Input type equals text, just like that. So now each one of these have an input after them. Let's just refresh our page and I'm gonna add some ⁓ text into that input and we'll click add item. You're gonna notice that the input is actually stayed the same for this top one instead of being you know pushed down to the bottom, like it should. You'd think that this would be the input, but you notice the inputs are just staying at the top instead of actually being pushed down because they're tied to this component. The reason for that is by default, React.

tries to figure out which one of your elements is associated with which item in your array. And to do that, it associates each index of the array with a specific set of components. So what happens is the index zero, which in our case is the first element, has this input element and it has this, you know, text or whatever associated with it. This entire return right here is associated with the index of zero. So when I add a new element to the beginning of my list by clicking this right here.

Sneha Mehra (00:04:13)  
You can see that what happens is that input that's still associated with index zero stays with index zero, even though you know the name is changing because the actual name element of our item is changing. So that's a big problem. So what you can do inside of React is you actually need to pass a key to every single element in an array. If we inspect our page, you're actually gonna see we have an error. We go to our console, each child in the list much should have a unique key prop. If you don't pass a key, by default, it uses the index, but you should never use the index unless it's the only option you have.

And even then, it's definitely last resort, which has bugs like this, as you can see. So what you need to do is you need to cast a key prop to each element. It's going to be the very first element you return, which in our case is our div. ⁓ And inside of here, you just need to make sure it is unique. In our case, all of our different elements have a unique ID, so we can say item.id. So now if I save, I'm going to refresh, I'm going to put some text inside of here, and I click add item. You can see that this input is correctly linked up with this item right here. So this key is just telling React.

what the actual identifier is. So no matter what changes in your array, whether the order changes, whether elements are moved around, doesn't matter what happens. It just says, okay, this ID is associated with this specific, you know, return right here. So it'll know that next time you render out that element, whatever the ID is, it'll match all of the different stuff that you need. So all the things like state and input values will stay the same and move around like you want them to.

Now this may seem really confusing, but really all React is doing behind the scenes is just hooking up your elements in your array with the unique keys that you give the elements that you want to return. So you just need to make sure that all the keys that you use are unique. You can't have the same key. For example, if I just said my key was one, obviously this is a problem. If I inspect, I'm going to get an error that's saying all the keys must be unique. But they only need to be unique for each instance of where you render this out. For example, if I rendered this text here twice, just like this, and I clicked save.

You can see if I inspect my page, I'm not going to get any errors at all. If I just refresh this, you can see I have no errors at all. No matter what I do adding items, everything is working perfectly fine with adding everything around. There's no errors. And the reason for that is because your keys only need to be unique for the actual instance that you're looping through. So in our case, each one of these loops right here is a separate instance. So even though they have the same keys between them, it's okay because they are separate. But each one of the instances needs to be unique. So in our case, all these IDs must be unique, which in our case they are because they're randomly generated.

Sneha Mehra (00:06:31)  
Another thing that's important is obviously the ID needs to stay the same between each one of the renders. And that's because if they changed every single time you rendered, React would have no way to hook things up, so obviously nothing would work. And it doesn't matter what you're doing here, whether you're returning a div, whether you're returning a custom component like item, you just need to make sure you always pass it, you know, the key like this. So if you had a custom component called item, you'd pass it the key just like this. You do not set the key inside the component. For example, if I had my item component, export.

Function item, I would not come in here and actually set the div or set the key. Sorry, so if I had a div,

Sneha Mehra (00:07:12)  
Close that off, there we go. This is not the correct way to do this. The key must always be defined where you're actually using the loop. So in our case, here's our loop right here. So that's where the key must be defined. It cannot be defined inside the actual child components. So this would just need to be moved to up here, and now it would work just fine.

—---------------------------

Sneha Mehra (00:00:00)  
Now in the last couple videos we really dove into how you can deal with complex state and how you can share state easily between multiple different components even globally across your entire application. This actually leads to a lot of problems where people share too much state globally or they don't put their state locally enough. So I've gone back to a really simple example. I just have an app that renders out a counter and inside of our counter we're storing the state for our account. This is super straightforward and easy. And if for example we wanted to add a reset button, it's really easy because we only need to change this one file.

For example, I just add a new button. I change this to say set count to zero. And I change this to say reset. And now I can click reset and it resets me to zero. So the really nice thing is since all of my state is stored inside this component, if I want to make changes to how that state works, it all happens inside of one single file. Super straightforward, super easy. Now the problem I see though is when people start to realize, you know, I can store state in context or I can store it, you know, much more globally, they do something like this instead. They take their count state and they put it inside their app.

And they take all the things for dealing with changing their state for their count and they put it inside of here. So they'll have a function that's like change count. We'll give it an amount. And it'll just call set count or takes the count and it'll just take the count and add that amount to it. Super straightforward. And then they'll pass in the count here. And they'll pass in the change count like this. Change count, there we go. And then inside the counter, they'll take that in. ⁓ Count, change count.

And this is going to call change count with a minus one. This would call change count with a plus one. And for now, we're just going to remove that reset. So if I do this, I make sure that I import use state inside of here and I say everything is working exactly the same as it did before. So it doesn't matter where you store your state, as long as you hook everything up properly, it's going to work just the same as before. But what I've done here is essentially I've taken my state and I've taken it out of the component where it's actually used, and I've stored it inside of a parent component.

And this is great if you need to share that state between lots of other components or do other information with it. But in our case, this state right here is used in one single place. It's used in this counter component. So why are we storing it inside of this parent component? Generally, inside of React, when you're storing state, it's best to store that state as locally as possible. And by that I mean essentially store all of your state inside the component that it's being used or as close to the components that it's being used in as possible. In our case,

Sneha Mehra (00:02:27)  
We can move all this state into our counter because that's where it's actually being used. And then it'll be as local as possible. And again, that makes it easy because you only need to change one single file when you need to update and make that state work differently. In our case, if we wanted to add a reset function, well I need to add that to my app here. So I'd set my count to zero. And then what I would need to do is inside of here, I would need to add that reset function as a prop, just like that. And then here I would need to take that in.

We go, and I would need to add a button called reset. That calls the reset function. And now you can see that reset function is working, but I had to change both my counter and my app in the GIN. That's because I stored my state essentially more globally than I needed to. I could have just stored it in my counter as local as possible, but I decided to put it in my app instead. Now obviously if you're looking at this, you're probably thinking, well, of course I'm gonna put the counter state inside the counter. And in this example, it's really easy to see that and see why that is.

But as you start to get to larger and larger applications with more and more state inside of it, it can become difficult because you may accidentally put your state in the wrong spot and you may not even realize that it could be more local than it is. That's something you always need to think about and consider is can I make this more local than it is? If so, probably about 90% of the time you should because it'll make your code easier to work with, since all that state will be localized and you won't have to worry about it in the more global state context.

This also works with the idea of context. People like to put essentially everything inside of a context, and that's not a good idea. Obviously, storing this counter information in a context doesn't make any sense at all because I only use it in one single component. So there's no point in putting it inside of a context. But even if I used it in a couple different components, it still may not make sense to put it in a context just because it doesn't need to be shared super deeply or super widely across my application. Context should really be used in situations where it is prohibitive to actually pass it down as props.

Because doesn't make sense to go through a bunch of different layers of prop drilling, or if it's something that needs to be used all over your application and like different areas of your application, single sections here and there, then it's a really good idea to use context. But otherwise, it's not a good idea just to put everything in context because then everything is global and it's really hard to deal with. Now there are situations though where local state can't actually be stored in the component that it's used. For example, if we actually changed our counter so that we had some other buttons in a different location, like for example, if we came into here, we just

Sneha Mehra (00:04:52)  
Give this so we can return multiple things. And instead I had a component called counter reset. And this counter reset is what's going to reset my actual counter. So let's create a really simple counter reset component.

Sneha Mehra (00:05:10)  
There we go. And inside of here, all I want to do is call a reset function. So we'll pass along that reset function. And it's essentially just going to be this button right here. I'm just going to move that into here. There we go. Now we can save our counter. It's no longer getting our reset passed in. And let's just make sure we import this counter reset. There we go. And it looks like in here my return is in the wrong location. There we go. So now what I've done, if we look at our app and we check it, you can see it looks like it's not working because I need to make sure I take a reset and I pass it to the right component. So

There we go. So now if I click reset, you can see it's resetting back to zero. So what I've essentially done is I've taken it so instead of our counter being just in one component, it's now in two components. Obviously, in this example, it doesn't make sense to break this apart, but imagine you're in a larger application where obviously it makes sense to break into two different components your logic. So now we have part of our counter here, and part of our counter state is being used here. So obviously, we need to store our counter state in a location where both of these components can access it. And in that case,

Storing it in this app component makes sense because this is the component that is the closest to where it is used. So this counter state is used in both these components. So obviously, this parent component is the one that is as close as possible to being as local as it can be. It can't obviously be stored in the counter because then we would have no way to reset it from this component. So we need to move it up to the parent. And essentially, when you're trying to make state as local as possible, what I like to do is I constantly try to put it inside of the next child, inside of the next child, inside of the next child, till I get to the point where

Putting it inside the next child, like putting it in this counter, no longer works because there's other components it's trying to access that need that data that no longer have access to that data. So this is a scenario where your state is as local as possible, but it's not actually stored inside the components that are using it. This is something you're gonna run into all the time, and something we've already run into all the time with the different like to-do list applications that we've built. We've had to store our to-do list inside of the app component because the other components, like the to-do list items,

Needed that information as well as the app component itself needed the information. Now overall, I don't want this to be something you spend too much time worrying about or thinking about because it's just a way to help make your code a little bit cleaner. But as you're writing your code, just have in the back of your mind the thought of, okay, is this stored as locally as possible? And if it's not, is there a reason that I'm not storing it there? Is it because it needs to be accessed by multiple components, or is it because it just makes more sense to store it somewhere else? But generally, if your storing state, the more local it is, the easier it is to change.

Sneha Mehra (00:07:33)  
and modify your code in the future, which is really what you want to have clean and well-written code.

—--------------------

Sneha Mehra (00:00:00)  
All I've done here is change our code to a class component. So now you can see our state is stored in a class component state and we're rendering out just like we would in a normal class component. And that's because I want to show you how to use refs inside of class components. It's actually incredibly easy. The first thing I actually want to mention though is how you actually store values that don't cause re-renders because it's different in class components. That's because you don't even need a ref to do this. I can just say this.value. ⁓

equals seven for example and now I have access to that and changing that value for example in here if I said that my value is now equal to eight that's not going to cause any types of re-renders anything like that. So if you want to store a value that is specific to your component but doesn't cause re-renders or anything like that just store it on the this property and that's going to be how you do that inside of a class component. But that's not what you're here for on this video. You're here to learn how to use refs inside of class components and it's really quite straightforward. If we wanted to get an input ref, all you would need to do is say React whoops

React.create ref and that's going to create you a brand new reference called input ref. And then we can just set it just like we did inside of a function component by saying ref is equal to that. And now we have our input ref. And what I could do is I could just come in here and I could say component will mount. Whoops, component ⁓ did mount. Sorry, there go. ⁓ And in here I could say input ref dot current ⁓ dot focus. You use the same current and everything. Everything works exactly the same. This is just how you get it instead of using a hook.

That's really the only difference. And now if I refresh my page and I make sure that I actually set this on the this property. Sorry. So this should be this.input ref. That way I can access it down here by saying this.input ref and this.input ref. Now if I save and I refresh my page, you can see it automatically focuses me inside of that input. So really the only thing you need to do is you just need to make sure you set a variable on your overall class, this object, by calling react.create ref, and then everything after that works exactly the same as it does inside of a function component.

The only difference is you don't need to use a reference for storing things like strings or objects and so on, because you can just do that on the object by just saying this dot value equals whatever you want it to equal.

—-----------------------------  
Sneha Mehra (00:00:00)  
Up until now, when we've been dealing with forms, we've pretty much been exclusively using state to deal with our inputs, which is fine and is how most people deal with forms inside of React, but it's not always the best way to deal with forms. For this example, I have really simplified code here. We just have a simple state for our name. We have a handle submit that'll just alert out our name as long as we type in a name. And then we have our form which has a label, it has our input, which has a value and an on change, which is updating our state for us, and then we have a button just to submit our form.

So if I just type in Kyle, for example, and I click alert name, you can see it prints out name of Kyle. And if I have no name and I click alert, it'll do nothing at all because of this simple if check here. This is really straightforward and a super common use case you'll see with forms. You can imagine that you have a bunch of state information, and then whenever you submit your form, you want to do something with that data, like call an API or something like that. In those cases, if this is all that your form is doing, it doesn't actually make sense to use state in those scenarios. For example, let's come in here and let's type out a use effect.

And want this use effect to run every single time my component renders. And I just want it to log out render, just like that. So now if we just inspect our page, we look at that console tab real quick. Make sure that I just clear everything out. And I give my page a refresh. You can see that it's printing out render twice just because of that strict mode. And now every time I change my name, you can see it's incrementing the count of my render, which is not a huge deal because it's not actually changing that much.

But it is causing my component to re-render every single time I type out a character in any of my inputs. But I don't actually care what the value of my name is until I submit my form. So there's no point in me constantly updating my state and re-rendering my component if I only need to get the information whenever some type of event occurs, like a handle submit. In those cases, it makes much more sense for me to actually use an uncontrolled input and use a ref to get the value instead.

Let me show you what that would look like. I'm just going to comment out the code for our current input information. I'm going to come in here with a brand new input. It's going to be very similar. So we're going to give it the same exact ID. We're going to give it the same exact type of text. But here, instead of doing a value in an on change, I'm going to be using a reference instead. So our reference here is going to be set to our input ref, or let's call it name ref, just like that. There we go. And I'm going to close that off. Then I'm going to come all the way up here. And we're just going to create that. So we're going to say const

Sneha Mehra (00:02:20)  
Name ref equals use ref, just like that. Make sure it's imported perfectly. So that looks really good. And now what I can do is where I used to use my name, I can just say that the const name is equal to name ref.current.value. Because that's going to get the value of my input. And now you'll notice that everything is going to work exactly the same as before. If I type in a name, hit alert, you can see it prints out the name. And if my name is blank and I click alert, it does nothing. But the big difference here, if I actually go to my console and clear everything out, is what happens when I

actually change my input. You can see we've rendered twice. Now it's clear that. And as I type in my input, nothing at all is changing. You can see I'm never doing a re-render at all. And then when I alert, it's obviously alerting that out. But again, there are no re-renders happening at all. My code is never being called again. So I'm actually saving a little bit on performance by using a ref as opposed to using state. And you would be surprised how commonly you create forms that work just like this where you only need access to the value whenever something happens, whether it's you submit your form or some other event happens.

It's not as often that you actually need access to the input all the time on every single re-render. So in cases like this where you don't need the input right away, I would recommend using a ref just to save a little bit on performance. And also it makes your code a little bit easier to read and understand because as you can see here, I got rid of this entire on change, which is kind of a pain to write. And all I have is this single ref here. So overall, it just makes the complexity of your code slightly simpler, and overall it improves your performance just a little bit.

—---------------------------

Sneha Mehra (00:00:00)  
So here we have the starting code for the exercises that you're going to do. You're going to notice we have a pretty lengthy README that explains exactly what's going on. The biggest thing you'll notice is this starting code is actually a full VET project, and it's very similar to the project we've been working on because as you can see, our child has a pretty much all the same code that we have here. And in our app component, I just have the ability to show and hire our child by clicking on this button that'll allow you to simulate mounting and unmounting.

Now, also a really important thing to note is in this main.js, I've removed strict mode, and hopefully you've removed it for all the videos so far up to this point. It'll just make me working with all these use effects a lot easier to understand exactly what's going on. Because if you leave it on, it'll sometimes double log things. And again, I'm gonna cover exactly how this strict mode works in just a couple videos, so then we can start using it in our projects. But if we look at the README here, you can see that in the top here, I'm just telling you how to get started with this project. Make sure you install all the dependencies.

And then you'll also notice that I just talked about this show hide button here and the removal of strict mode. Now, for the actual things that you need to do, there's some instructions here, these first four steps that you should definitely do. And then I have two bonuses which are a little bit trickier, but I think you can do them. And the only reason they're trickier is because they're covering things that we haven't actually covered directly in any of the video yet. But hopefully, with a little bit of thinking and application, you can figure out these. But if you can't, again, no worries. So the very first thing I want you to do is I just want you to log out the text render every time the component re-renders.

Then log out the text high when the component mounts, then log out this text right here whenever the name or age changes, and update the document title anytime the name changes as well. For our bonuses, I want you to log out by when the component unmounts. Again, a little tricky because we haven't covered it. And then this one is a little confusing, so I want explain what's going on. Essentially, I want you to use a set timeout to log out the text my name is and whatever the name is, only after there's been a one-second delay in typing. So for example, if I come in here and my text starts out as Kyle.

And I really quickly delete two characters like that, you can see that there is less than one second between each change. So what happens is it waits until there's one second of delay between changing the name and then it'll print out whatever the current name is. So in our case, if I have Kyle here and I really quickly delete two characters, it'll print out ky after one second. Now in the other hand, if I delete the E and then wait a while, and then delete the L and then wait a while, it'll print out both KYL and KY one second after each one of those changes.

Sneha Mehra (00:02:16)  
If it's confusing, just read through this again and hopefully it'll make a little bit of sense. Essentially I'm just making a delay that's saying after one second of no changes at all happening in my input, print out whatever the value of the input is. That's what that final one is doing. And again, it's something we haven't covered, but hopefully you'll be able to figure it out.

—----------------------

Sneha Mehra (00:00:00)  
So to get started, I want to just go ahead with the very first instruction here. In this one, I want to render out or I want to log out the text render each time the component re-renders. So what we can do inside of our child is we can come in here, we can say component did update. And I would just want to console ⁓ dot that we have rendered. So now if I inspect my page, I go to my console and I change something, you can see it's printing out render, but on initial mount it's not printing out render.

Now based on my instructions, it's little bit vague, vague whether or not you want this to log or not, but since I want it to work the same as use effect, I want this to render out on mount as well because that's how use effect would work. So in our mount here, I'm also going to do console.log of render. And again, this is another reason I don't like these class component way of doing things as much, because you have a lot of duplicated code. But now if we inspect our page, go over to our console and refresh, you can see on mount it'll print this out, and on every single change it'll also print this out.

Now speaking of mounting, I think our next thing here is to print out high when we mount. So we can do that real quick. I'll remove these other files we don't need. So here I can just console.loghi. And now if I inspect our page, give it a refresh, you can see it logs out high, and I'll do that every single time I mount. But on change, it'll only render out that text render. Back over to the README, we want to do something when our name or our age changes. So we can come into our did update and we want to get in those previous state. So we'll get previous props.

And the previous state. ⁓ And I want to do a quick check if our previous state.name is not equal to this.state.name ⁓ or our previous state.state.age. Well that means either our name or our age has changed. And I want to log out that text. So I'll just copy that straight from the readme so I don't to type it out twice. There we go. Paste that in here. So now this is going to log whenever our name or our age changes.

So if we inspect our page, we just clear everything out, refresh, and we change our name. We're immediately getting an error. Let's just see what this is saying. ⁓ yeah, here it needs to say this.state dot age, and this needs to say this.state.name. That'll fix the code. Again, another reason I don't like class components as much, they are much more verbose. So refresh here, change our name, you can see it's printing this out, change our age. Again, you can see this is printing out. So it's doing exactly what we would expect, which is great.

Sneha Mehra (00:02:25)  
Moving back on here to the next, update the document title every time our name changes. So we need another if statement. And this one is specifically for if our name changes. So if our name is not equal to our old name, then we can change that title.title is equal to this dot ⁓ state.name. There we go. Again, another reason I don't like doing this is we have all of this inside of one function, and it's difficult to parse and read exactly what's going on. But if we change our name.

You can see up here in our document title, it is also being updated, which is exactly what we expect. Now, moving on to the bonus sections, the unmount one is luckily much easier than it was in the use effect version because we just have component will unmount. ⁓ Super straightforward. We can just console.log by. There we go. So now if we inspect and we unmount, you can see it logs out by every single time we do that. Super straightforward, really easy.

And now we get onto this timeout section, which is even harder than it was in use effect, in my opinion. So what we want to do is anytime our name changes, I believe is what it was. Yep, we want to check that one second delay. So we right here are already checking if our name changes. And in here, I want to set a timeout. So we can just set a simple timeout. And this timeout will just console.log. ⁓ My name is this.state dot name. There we go. And we want this to happen only after a one second delay.

And again, if we keep it just like this and we don't do anything else, it's obviously not going to work because after a second, it's going to print out a bunch of different things. You can see it printed out seven different times. Definitely not what we want. So instead, we need to make sure we clear out the timeout every single time we enter into here. So we need to store this in a variable. But we can't just store it in a const variable because this is going to be reset every time we call this update function to a new variable. So obviously that won't work. We have no cleanup function to work with.

So instead we need to set this as a variable. We'll call this our name timeout. ⁓ And we'll set it equal like this. So what this is doing is just setting on the this property. So it doesn't matter. We can just set whatever we want on here. This is just a way to store variables that we can use at a later point. And then what I can do is I can say if ⁓ this dot name timeout is not equal to null, well then what we can do is we can clear it. So we can clear our timeout ⁓ by passing it our this.name timeout.

Sneha Mehra (00:04:46)  
So now hopefully if I come over to my console, change my name a bunch, you'll see it'll just give us one log after we have a second delay of changing our name. So that's exactly what we expect and should have happen. But again, we need to make sure we clean this up inside of our unmount as well, because this doesn't automatically happen. So we'll do the exact same thing for our cleanup right here as well. So again, another reason why I really don't like working with class components when it comes to doing a lot of updates like this. That right there though takes care of all of the code for everything inside of our README.

Again, if you weren't able to get this timeout section done, that's 100% okay. I mean it's complicated code and it's not super obvious how it works, so don't feel bad at all. Even if you weren't able to get some of these normal ones, again, don't feel bad. Just go ahead and re-watch the video for the life cycle methods at the section where you failed. So if for some reason like mounting was what threw you off, just watch the section where I talk about this mount function and hopefully it'll make it a little bit easier to understand.

—--------------

Sneha Mehra (00:00:00)  
In the last section of the course, we talked all about how you can add state to components to make them interactive. And now in this video, we're going to talk about how that actual interactivity works by talking about the life cycle of a component. And the very first thing we need to talk about is the virtual DOM, which is how React handles and stores all the information about what it renders out to the screen. This sounds like a confusing topic, but I promise you it's pretty easy to understand. Now I've taken the project that we had before where we had the name input and the

Age input, I remove the age input so all we have is the name component inside of here. And then also we have our app and inside of here I have a button that is rendering out a count, just so we can update both the app and the child component separately. So for example, if I click on this button here, that's going to be rendering out changes to my app. While if I type in this input, that's going to be rendering out changes to my child component, because this input is in the child component and the button is inside of the app component.

I also added a few logs here. So inside of our app, whenever it calls our app function right here, it's going to say render app. And in our child, it's going to say render child every time it calls this function. Now I already talked a bit about how when you update the state inside of a component, it reruns the entire component from top to bottom. So if I click D to re-render my name to change that state, you can see it prints out render child again because it's recalling this entire component, which is giving us this console log right here. This is pretty straightforward to understand.

But what makes it a little bit more confusing is let's say we change something in our app. So we click on this button right here, that'll cause it to change our state, which re-renders our entire app component. And our app component also contains our child component, which means our child will need to re-render as well. And any children inside of that will also need to re-render. So if I clear out my log here and I just click this button, you'll notice it prints out render app and render child because it calls this entire function. And inside this function we have our child component. So it goes into our child component and runs this entire function again.

So every time you make a change in React, it's going to call every single component that is a child of that component that you're inside of, as well as the component that you changed. So it could do a lot of different render calls inside of your code. Like a lot of your functions might run many, many times whenever you make changes in React. And normally if you've worked with the DOM, you know that making changes to the actual DOM is pretty slow.

Sneha Mehra (00:02:10)  
For example, if I click this button and I had to re-render the entire DOM for my application, like all of this code and all of this code inside this return, that could be pretty slow, especially if your DOM is quite large and complex, which is why React uses a virtual DOM instead to speed this up.

So the entire idea behind the virtual DOM in React is it essentially takes the DOM, so all the code that you have here for all your HTML, and it stores all of that in memory somewhere. And then any time that you make a change, for example, I click this button and re-render my app and my child component, what React does is it says, okay, I'm going to run those functions. So it runs your app function and then it runs the child function inside of that. And then it gets the output. It says, okay, what is the DOM going to look like? And it takes this new version of the DOM that you get from running both these functions and it compares it to the old version that you have.

Before. That's like your virtual DOM. So it's comparing what you used to have with what you now currently have. And it finds out, okay, where are the differences? In our case, when we click this button, the only thing that changes is the text inside this button. So React says, okay, the text inside this button has changed, so I'm only going to change the actual DOM by modifying the button. So the virtual DOM is like a fake version of the DOM. So it's like a JavaScript interpretation, it's just stored in memory what the DOM should look like. And then React takes that version of what the DOM should look like.

versus what the DOM actually looks like and it only updates the thing that changes. A really easy way to see that is if we go into our dev tools here, if I go to the Elements tab, and I'm actually going to bring this over to our page over here. So let's just refresh our page completely. We'll open all of these up so we can see exactly what our DOM looks like. And as you can see, this div right here that says class of child, that's where all of our child component code is at. And then everything else inside this other div here is our app component. So this button is inside our app and then all this code is inside of our child. So we know for a fact when we click this button to change our text in here,

The only thing that should update is the text of our button. And if you look on the left side of my screen, this little zero right here is going to flash whenever it changes. And that's just something that dev tools have built in. So every time the actual DOM changes, it'll flash to let you know what is changing. So you can see, even though my app is rerunning and my child component is rerunning, it's not actually putting all those changes in the DOM. It's not overriding everything. All it's doing is updating the things that have changed from the last time it rendered. So in our case, only the button is changing. If I change my name down here,

Sneha Mehra (00:04:23)  
You'll notice there's two places that change. The value of my input changes as well as the text that's being printed out down here. Because when I run that child component, those are the two sections of the DOM that change. So the whole idea of this virtual DOM is React stores it in memory, what your code actually looks like, and then it allows it to render out all your components to make sure everything looks exactly like it should. So it runs all of your code, which is relatively quick to do, and then it compares what should it look like versus what does it look like, and it only changes the things that need to change, and that makes React much quicker.

Then if you just did a blanket replacement of every single thing in the DOM, that would be incredibly slow. The best way to think about this is in three steps. Whenever your component renders, what happens is it runs all of the code inside your function. So it's going to run all this code right here. Then what it's going to do is it's going to compare the old version versus the new version, that's step two. And step three is it applies all of the changes that need to happen to make the old version look like the new version. So first run the function, then compare, then make updates.

Also, this process will work exactly the same for a class component. The only difference is that a class component just calls your render function over and over again. So this is the only difference. The render function here is what gets called every time your component re-renders, and in a function component, just the entire function is what gets called.

—----------------

Sneha Mehra (00:00:00)  
Now the last state-based thing that I want to talk about before we move on to our first project for this section is how you actually deal with state that is dependent upon which environment you're working in. So something like an environment variable. Just take for example this simple application. It does really nothing except for render out a URL and imagine that you want to render a different URL on development versus production. This is really common if you're dealing with calling an API because when you're in development, you want to call your development API. While you're in production, you want to call your production API.

You want that to be able to automatically change based on different environment variables. Well, this is actually really easy to do whether you're using vt or create react app, and I'll show you how to do it with both of them. First, we're going to get started with vt, and in order to use environment variables, all we need to do is to create a .env file. So we can say .env, and inside of here we can put any environment variables that we want. So for example, I could just say URL and I could set it to www.dev.com, and that's going to be that URL saved inside of an environment variable. There is one slight problem though.

VET is really nice in that they don't want you to accidentally leak sensitive information. Like I could have like a you know password inside of here, which is just password one, two, three, or something. And obviously, I don't want this password to be shown on the client because anything that you put inside of your client is available to anyone and they could easily get this password and hack your site. So VET is really nice in that it'll only expose environment variables that start with VT underscore. Anything else will be completely ignored and not actually exposed to your client.

So this password is never going to be available. We can see that by accessing our environment variable here. So instead of hard coding this, we can change this to import.meta.env dot whatever we called it. We called this vURL. So we're going to paste in vURL. And if we save, you can see our output hasn't changed at all. If I come in here and I just say just dev2 and I give this as change, you can see it's now dev2. So it's perfectly synced up with our environment variable. But if I tried to access that password,

And I save, you'll notice nothing gets rendered to our screen. And that's again because it'll only expose environment variables that start with VT underscore. So that's really important. That'll make sure it won't expose sensitive information by accident. Also, Vit makes it really easy to set up different environment variables for different production environments that you have. So for example, we have our.env, and if we wanted to put sensitive information in here, we probably don't want this to be checked into our GitHub repository. So by default, they ignore every file that ends with.local. So you can change this to.env.local. ⁓

Sneha Mehra (00:02:25)  
And now this will not be checked into any git repository. As you can see, it'll be ignored by the git ignore right here. So this is something you're going to want to do if you're going to put sensitive information inside of your env variables. Also, you can change your env based on different environments. For example, I could go to.env.development, and now whenever I'm in development, it'll run this environment variable code. Otherwise, it'll fall back to this one. I can do the same exact thing for production. So I can come in here and I can change this one to dot production.

And now it's saying that use this in production, use this in development, and use this one in any other scenario. So inside of production, let's say I change this URL to production.com. So let's just get rid of this normal generic one and all I have is development and production. And if we go back to our application and we get that VT URL, and I just do a refresh, you can see it's currently showing me the dev one because I'm in my development mode. Now, if I were to come in here and I change this, so I just run npm run build.

That's going to build my site for production. So it's going to do everything in production mode. And I say npm run preview. That's going to run whatever I just built. I open that up. You can now see we have the production URL being used. So my actual code did not change at all, but I have different variables set inside of my different environment variables and it's using the correct one based on what environment I'm in. This is really useful because when you're testing, you can use this environment.development.local to set up all your different local testing, development-based URLs and so on.

And then when you deploy your site, almost every site that you deploy to is either going to have some type of environment variable UI you can configure the environment variables in, or you can just create a.env.production file, and that's going to set up all your environment variables for you that way. So no matter what happens, you can have different data in development and in production, which is incredibly useful. And environment variables are going to be something you use in pretty much every single project. Now I just went through and created a brand new Create React app application to show you how this works with.

create React app instead of Vit, and it's almost identical. You'll notice in the git ignore, they already ignore all of your.local files. So your ENV works the same with that dot local syntax. So we can come in here, create a.env dot development dot local. That's going to work all the same. So all of the naming works exactly the same. And let's say that we wanted to create a URL. Well in Vit we would do VT underscore URL.

Sneha Mehra (00:04:41)  
In React, all we do is say react underscore app underscore whatever the name is in our case URL. So we'll say www.dev.com. And then also I'm going to put in this password one in here. So we'll just say password one two three just to show you that this is not exposed. Now what I can do is I can convert this. Inside of V, you would say import.meta.env, but inside of React, we say process.env dot whatever it is. In our case, Reactapp URL. Now if I save.

You'll notice it looks like it's not working, but that's because in React you actually need to restart your server every single time you change your environment variables. So I give that a quick restart real quick. Now if I refresh my page, you can see that that is properly showing up like I expect. And if I come in here and I were to change this to dev2, again, I'd have to restart my server to see those effects take effect. Now what I can do is I can come in here and I can try to render out that password. And you're gonna notice when I do that, it doesn't render anything. And that's because this is hidden by default since it doesn't start with React.

underscore app underscore. Other than those two minor differences where you must call it with process.env and start it with react underscore app underscore, they work exactly the same as NVIT, so you can use them exactly the same way with creating your environment variables. Just know that you need to restart your application anytime you make changes to your environment variables, which is a little bit of a pain.

—--------------------

Sneha Mehra (00:00:00)  
Now the last state-based thing that I want to talk about before we move on to our first project for this section is how you actually deal with state that is dependent upon which environment you're working in. So something like an environment variable. Just take for example this simple application. It does really nothing except for render out a URL and imagine that you want to render a different URL on development versus production. This is really common if you're dealing with calling an API because when you're in development, you want to call your development API. While you're in production, you want to call your production API.

You want that to be able to automatically change based on different environment variables. Well, this is actually really easy to do whether you're using vt or create react app, and I'll show you how to do it with both of them. First, we're going to get started with vt, and in order to use environment variables, all we need to do is to create a .env file. So we can say .env, and inside of here we can put any environment variables that we want. So for example, I could just say URL and I could set it to www.dev.com, and that's going to be that URL saved inside of an environment variable. There is one slight problem though.

VET is really nice in that they don't want you to accidentally leak sensitive information. Like I could have like a you know password inside of here, which is just password one, two, three, or something. And obviously, I don't want this password to be shown on the client because anything that you put inside of your client is available to anyone and they could easily get this password and hack your site. So VET is really nice in that it'll only expose environment variables that start with VT underscore. Anything else will be completely ignored and not actually exposed to your client.

So this password is never going to be available. We can see that by accessing our environment variable here. So instead of hard coding this, we can change this to import.meta.env dot whatever we called it. We called this vURL. So we're going to paste in vURL. And if we save, you can see our output hasn't changed at all. If I come in here and I just say just dev2 and I give this as change, you can see it's now dev2. So it's perfectly synced up with our environment variable. But if I tried to access that password,

And I save, you'll notice nothing gets rendered to our screen. And that's again because it'll only expose environment variables that start with VT underscore. So that's really important. That'll make sure it won't expose sensitive information by accident. Also, Vit makes it really easy to set up different environment variables for different production environments that you have. So for example, we have our.env, and if we wanted to put sensitive information in here, we probably don't want this to be checked into our GitHub repository. So by default, they ignore every file that ends with.local. So you can change this to.env.local. ⁓

Sneha Mehra (00:02:25)  
And now this will not be checked into any git repository. As you can see, it'll be ignored by the git ignore right here. So this is something you're going to want to do if you're going to put sensitive information inside of your env variables. Also, you can change your env based on different environments. For example, I could go to.env.development, and now whenever I'm in development, it'll run this environment variable code. Otherwise, it'll fall back to this one. I can do the same exact thing for production. So I can come in here and I can change this one to dot production.

And now it's saying that use this in production, use this in development, and use this one in any other scenario. So inside of production, let's say I change this URL to production.com. So let's just get rid of this normal generic one and all I have is development and production. And if we go back to our application and we get that VT URL, and I just do a refresh, you can see it's currently showing me the dev one because I'm in my development mode. Now, if I were to come in here and I change this, so I just run npm run build.

That's going to build my site for production. So it's going to do everything in production mode. And I say npm run preview. That's going to run whatever I just built. I open that up. You can now see we have the production URL being used. So my actual code did not change at all, but I have different variables set inside of my different environment variables and it's using the correct one based on what environment I'm in. This is really useful because when you're testing, you can use this environment.development.local to set up all your different local testing, development-based URLs and so on.

And then when you deploy your site, almost every site that you deploy to is either going to have some type of environment variable UI you can configure the environment variables in, or you can just create a.env.production file, and that's going to set up all your environment variables for you that way. So no matter what happens, you can have different data in development and in production, which is incredibly useful. And environment variables are going to be something you use in pretty much every single project. Now I just went through and created a brand new Create React app application to show you how this works with.

create React app instead of Vit, and it's almost identical. You'll notice in the git ignore, they already ignore all of your.local files. So your ENV works the same with that dot local syntax. So we can come in here, create a.env dot development dot local. That's going to work all the same. So all of the naming works exactly the same. And let's say that we wanted to create a URL. Well in Vit we would do VT underscore URL.

Sneha Mehra (00:04:41)  
In React, all we do is say react underscore app underscore whatever the name is in our case URL. So we'll say www.dev.com. And then also I'm going to put in this password one in here. So we'll just say password one two three just to show you that this is not exposed. Now what I can do is I can convert this. Inside of V, you would say import.meta.env, but inside of React, we say process.env dot whatever it is. In our case, Reactapp URL. Now if I save.

You'll notice it looks like it's not working, but that's because in React you actually need to restart your server every single time you change your environment variables. So I give that a quick restart real quick. Now if I refresh my page, you can see that that is properly showing up like I expect. And if I come in here and I were to change this to dev2, again, I'd have to restart my server to see those effects take effect. Now what I can do is I can come in here and I can try to render out that password. And you're gonna notice when I do that, it doesn't render anything. And that's because this is hidden by default since it doesn't start with React.

underscore app underscore. Other than those two minor differences where you must call it with process.env and start it with react underscore app underscore, they work exactly the same as NVIT, so you can use them exactly the same way with creating your environment variables. Just know that you need to restart your application anytime you make changes to your environment variables, which is a little bit of a pain.

—--------------------------------

Sneha Mehra (00:00:00)  
In the last couple videos, we kind of touched on event listeners a bit, but I want to go a little bit more in depth in event listeners, especially when dealing with input events and how the value property actually works inside of React. ⁓ Now I've already talked about when you want to do an event listener, you just put on and then whatever the event you want to do is. So for example, if we want to do like on mouse down, for example, we can just say on mouse down, pass it in our event, and now when my mouse goes down, you can see that the event fires.

The main thing you'll notice is everything is camel case based and you just always pass it a function. It's pretty straightforward for the most part. But what happens if you want to hook up an input to some type of state variable? So let's say we're gonna have an input here where the type is text, and we're just gonna close that off and we wanna hook it up to our name input here. So we're gonna set the value equal to our name, just like that. We're gonna remove this handle click and remove this age because we don't need it. So now we have an input where the name is set to Kyle, because that's our default value.

But I can type all I want, I'm hitting a bunch of keys, and nothing is happening. But that's because our name state never changes. Remember, React is declarative. So that means we declared that the value of our input is always our name. And we have no way to update our name. So obviously our input never changes its value. That's different though between ⁓ default value. I kind of mentioned this a little bit before. The default value is saying we're declaring that the default value of our input is our name, but we can change it to whatever we want.

Because we're only setting the default value, not the actual value of our input. But in our case, we want to set the actual value. So how do we get the value when it changes and update it? This is where the on change event comes in. Normally, if you're used to JavaScript, you may know that the change event only fires after you're done typing in an input. And normally you would use the on input event because every time you click a key, that's going to call the on input event. Well, React decided that that was a little bit too confusing.

So they essentially made it so that the on change event does the exact same thing as on input. So every single time you make a change, whether you hit like backspace, hit a new key, whatever you do inside your input, it's always going to fire the on change event. ⁓ And this is going to take in an event object. ⁓ In this event object, we just say e.target.value to get the current value of the input. So I can set our name to that value by saying set name here and passing it that value. And now what's going to happen is every time our input changes, we're going to update our name.

Sneha Mehra (00:02:13)  
Which is updating the value of our input and it's gonna kind of loop around like that. So if I just type F, you can see it now updates my variable to that. And again, the way this is working is because it's updating our state variable. When our state changes, we re-render our component. So we're redoing this entire app and we're saying, okay, our value here is equal to our name. Our new name has an F at the end of it, so it's rendering Kyle with an F at the end of it. This is one thing that really trips up a lot of people with React is the fact that inputs kind of work a little bit differently than they do in the browser because

I can type all I want inside of here, but if I don't have this on change hooked up, it's never going to change my value. Also, like let's say I just remove this real quick, ⁓ you'll also notice we're gonna get some errors in the console. So if I refresh and inspect, go to the console, you can see it specifically says, hey, I gave it a value, but I never gave it an on change. So it's making sure that I actually understand what's going on and make sure I put an on change when I have a value. When you're doing research for this topic, you may come across the terms of controlled and uncontrolled inputs. This is an idea from React.

Where it's saying if you set the value and you set the on change like this, this is considered a controlled input. And that's because React is controlling everything to do with this input. Because if I don't have an on change, for example, and I type, nothing happens. The browser does not have control over it. Instead, React has control over what is inside my input. If instead though you decide to use a default value here with no on change, well now you're saying that the browser has control of what's in that input, and I can type just fine.

But it doesn't update any of my state variables. For example, this name variable never gets updated. So if you're using value and on change, you're using what's called a controlled input because React is controlling what the value of that input is. While if you use something like default value, then you're saying that the browser should control what's in the input and React is not controlling it, so they call that an uncontrolled input. That's one important distinction to understand between that nomenclature, because you're probably going to encounter that if you're doing any research for React.

—-------------

Sneha Mehra (00:00:00)  
Now the next thing that I want to talk about is how you can actually do conditional rendering inside of React, essentially rendering something based on some other type of input, whether something is true or false. The way that we've done this in the past is by just using a simple if statement outside of our return. So for example, here we have my favorite number is and we're passing a favorite number, and this is coming in from our props, which we're passing in from our app here. As you can see, we're passing in three.

But if we don't pass in a favorite number, I don't want this string to show up because it doesn't make sense to have the string if there is no favorite number. So I should use an if statement to do that. So we can come in here. And if we can just sum down here, we can say if favorite number ⁓ is equal to null. Then what we can do is we can set some JSX. So we'll just say let JSX equal nothing. So here our JSX is going to be equal to. Whoops. My favorite.

number ⁓ is and we can put in that favorite number just like that to make sure I close this off properly and then we can render that out instead of our string down here. And now if we do that and we make sure our check here is not equal to null you can see it no longer shows that but when we pass in a favorite number of three for example you can see it's now rendering out that string. So this is one way we can do this but this is obviously a little bit convoluted. It's quite a lot of code we have to create a full if statement

And we need to create a custom variable to contain this. So inside of React, instead what you can do is actually inline all of these different checks. And the most common way that people are going to do this, if we just bring this back to what we had before, remove all this code. So we just have our my favorite number down here, is what you're going to do is you're going to wrap this inside of the curly braces. That's saying that you want to execute some type of JavaScript code. And then we're just going to put our if check right here. So we're going to say favorite number, not equal to null. ⁓ And then this thing.

So if our favorite number is not equal to null, then we're going to run this code. So I'm just gonna put this here inside of our brackets like we had before with our string and inside of this. And now if I give this a save, you can see it's rendering out just fine. And if I remove this, you can see it removes that statement. So I'll go over exactly why this works as it is, and it's using something called short circuiting inside of JavaScript. If you're unfamiliar with the concept, I'll link a video on it in the description for you that goes more in depth. But essentially what happens is when you're running JavaScript code,

Sneha Mehra (00:02:18)  
It's checking this, it says favorite number not equal to null. And right now, immediately, if our favorite number is three, for example, this evaluates to true because our favorite number is not equal to null. So then JavaScript runs the other side of our and statement and it returns whatever this is. Because when you run an AND statement like this, it just takes whatever the last thing is inside the statement, and that is what's actually returned from the statement. So this line of code will return this last section right here. Now, if our favorite number is null though,

So if our favorite number is null, then this evaluates to false. And when you're using an and statement, anytime you get a false value, what happens is it immediately returns and it returns the value of false. So this entire statement right here evaluates to false. And it doesn't even run the code after the double AND because it already says, well, I have a false value. And a false value and anything else, whether it's true or false, is always going to be false. So it just doesn't even bother executing this code because it knows no matter what this does, it's always going to be false. So this executes to false.

It returns false and then false doesn't render to anything on the screen as we already know by JSX. If ⁓ you know try to render false, it just renders nothing. So in our case it'll render nothing if our favorite number is null. The way to think about this type of syntax is if code before the double ampersand is true, then what we do is you know render out this. Otherwise, if the code before the double ampersand is false, do nothing at all. Many people will actually take this code and try to shorten it up a little bit, and instead what they're gonna do is this. They'll say, if we have a favorite number, then do this code.

As you can see, this seems to be working just fine. If I remove my favorite number, you can see here it removes itself. So you may think that this is working fine, but this is a little bit of a problem with how JavaScript renders truthy and falsy values. If we change our favorite number to zero, you can see that it's not actually rendering out to our screen. It's just rendering the text zero. This is kind of weird exactly, you know, what's going on. Well, what's happening here is like I mentioned, we're taking our favorite number and we're saying, is this a true or false value? That's all this is checking. And zero is considered falsey. So it

takes that zero and it returns that zero, which in our case is just rendered out to the screen. So we have our zero showing up right here. While if we had a different value such as three, well three is a truthy value. So this evaluates to true. So then it just executes this final section down here. So I recommend never actually doing this with your code where you're writing out just this to check if something exists because you can run into problems where like if you try to render zero, it doesn't exactly look like you want it to. So there's a few ways you can get around this.

Sneha Mehra (00:04:35)  
The first is you can put a double you know exclamation point in front of your number here, and this is going to convert from a you know number like a zero to a Boolean. And if we do this, you can see it remove the line. But again, this doesn't quite solve our problem because zero can be our favorite number and that is a falsy value. So in those cases, what I recommend doing is doing the explicit check for null like I was doing. Now, another thing that you're commonly going to run into is the idea of using ternaries inside of here because sometimes you may want to do something based on a value. So we can come in here and say if our favorite number is greater than five.

Then we want to execute some type of code where we can say my favorite number is large. Just like that. And then we want to execute some other code if it's not. So in our case, we can say my favorite number ⁓ is small. There we go. So now you can see as my favorite number is small because it's less than five. We change this to 10\. You can see this as my favorite number is large. What we're doing is we're inlining a ternary statement. So we have our if check here. If this is true, we run the code after the question mark. Otherwise, we run the code after the calling. That's just how ternaries work.

If you're unfamiliar with them, again I'll link a video for you in the description that covers ternaries. This is something that's really common that you're going to see inside of your React code, is you're going to see these types of ternaries, as well as you're going to see that, you know, double ampersand trick. So understanding how these actually work is really important because they're going to be something you use all the time and you see all the time. And it's important to note you don't just have to return a string. For example, I could return anything. I could return a div, you know, and inside that div, I could just have some other code. So I could have a div that you know has some code inside of it, and then that can have a span which has some code inside of it.

Now you can see that it's rendering out all that different code for us. So you can see you can put a lot of different stuff inside of here if you want. I recommend not getting too complex when you're doing this inline kind of ternary stuff, but it's okay to render out a few different components inside of it like we're doing here.

—------------------  
Sneha Mehra (00:00:00)  
This is just a really quick update video, and that's because if you created your own Vit application, you may have noticed that you had one additional file that I didn't, this ESLint file right here. And that's because Vit recently updated and added an ESLint file literally days after I finished recording those videos for the course. So this is just an update video to talk about what this file is and if you should worry about it or not. Now if you're unfamiliar with ESLint, essentially the whole idea behind ESLint is it's going to give you different warnings and errors inside of your code.

For different things that you do, such as defining a variable and not using it. For example, if we just come into here and I create a variable, let's just call it component. Actually, let's just call it test. ⁓ And we'll just set it to high. You'll notice I get this red squiggly line that's saying, hey, this is declared but never read. And it's giving me a warning right here, and you can see it says eslint and then the actual name of that error. And I can actually click on this, open this up, and if I bring it over, you can see it's going to give me a a really long documentation of exactly what that specific error is, telling me exactly what I need to do. So the one in particular.

Is right up here and it tells you tons of information about how to fix it and what the error is. ⁓ So there's different rules that ESLint has set up that are going to essentially lynch your code and tell you if different things are not proper. Like having a variable that you don't use is probably not a good idea. So it's saying that you should remove it or make sure you use it somewhere. Now the one problem with ESLint is oftentimes it's a little bit too strict and it adds in extra rules that you maybe don't want. Because by default, Vit has all these different rules that it adds in, a bunch of different rules around React and then just normal ESLint rules.

But some of these rules, like I said, you may not want. One of those rules in particular, which I'm going to recommend that you actually disable, if we open up this component here, I know we haven't really talked much about components, but you're noticing I'm getting a little bit of a red underline. If I hover over this, you can see it says title is missing in prop validation, and then it's telling me this is a React Prop Types error.

In this course, we're not going to be really doing much with TypeScript, which is the best way to do type validation inside of React. And the other alternative is something called prop type, which is not covered until much, much later in this course, and it's really not that important because you should probably just be using TypeScript instead. But since we're not using TypeScript in this course and we're not going to be using Prop Types because it's just an inferior thing compared to TypeScript, I recommend disabling this error. And the way that you disable errors inside of ESLint is incredibly easy. You just find the error you want to see, have for example this error.

Sneha Mehra (00:02:13)  
And you look and you'll see on the very right side it'll say ESLint, followed by whatever the name of the error is. In our case, the error is called React slash prop dash types. So we can go into our ESLint in this rule section. If you don't have this, just add it in yourself. And what you can do is you can just type in react slash prop dash types. We can put a colon and then we can say what we want to do with that error. And there are three different options. The first is off, and that just completely removes the error. And as you can see, I no longer get an error for that.

The second option is warn, and that's just going to give me a yellow warning as opposed to an actual error. And then finally, what it was already set to was error, which is going to give you that red squiggly line. So in our case, I would recommend just setting this one to off. Otherwise, you're going to get a bunch of errors that are going to come up for stuff that's really not super important at all. But again, you can play around with this as much as you want. Alternatively, you can use zero to define off, one to define a warning, and two to define an error if you prefer to use that style of syntax over the actual like words.

Now, if you don't actually like ESLint and you want to remove it, it's super simple. All you need to do is just go to your ESLint file, delete it, and now you don't have to worry about ESLint at all. And in your package JSON, you may want to remove this line right here, which is actually doing your linting for you. And you can remove all the different dependencies for ESLint as well. But essentially, if you just remove that file, that's going to be enough to actually get rid of all those ES Lint warnings and errors. So you don't have to worry about them if you don't want them. But you can keep it in there if you want them and just disable the different warnings that you don't actually like. Again, it's entirely up to you.

The ESLint is really this there to try to help you, but sometimes it gets in the way and you may need to disable different warnings or add in different ones that you actually like.

