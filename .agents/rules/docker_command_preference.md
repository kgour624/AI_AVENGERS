# Command Preferences

- **Docker Executable**: Always use `docker.exe` or `docker-compose.exe` (with `.exe` extension and hyphen) because Windows shell treats bare `docker` as an unassociated file path on the user's system and triggers a "How do you want to open this file?" dialog.
